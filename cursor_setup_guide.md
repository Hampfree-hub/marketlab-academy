# 🚀 Cursor & Windows: Оптимизация терминала

Если Cursor зависает на `git` командах или выдает ошибки кодировки в Windows, следуйте этой инструкции.

## 1. Установка Git Bash (Рекомендуется)

PowerShell часто конфликтует с AI-ассистентами из-за сложности обработки аргументов и кодировки UTF-8. Git Bash работает в 10 раз быстрее и стабильнее.

1. Установите [Git for Windows](https://git-scm.com/).
2. В Cursor: `Ctrl+Shift+P` -> **Terminal: Select Default Profile**.
3. Выберите **Git Bash**.

## 2. Использование Node.js скриптов для Git

Если терминал всё ещё капризничает, используйте готовый скрипт, который гарантированно обходит проблемы оболочки:

```bash
# Использование:
node scripts/git-commit.cjs "ваш комментарий к коммиту"
```

Этот скрипт автоматически сделает `add .`, `commit` и `push origin main`.

## 3. Настройка кодировки (UTF-8)

Чтобы русские буквы в GitHub Issues не превращались в "кракозябры", мы используем Node.js для запросов к API. Все скрипты в папке `scripts/` уже настроены на правильную работу с UTF-8.

## 4. Как избежать зависаний (Советы)

- **Разбивайте команды:** Вместо `git add . && git commit` пишите их по отдельности.
- **Используйте Bash-стиль:** Даже в Windows, Git Bash понимает `/c/Users/...` и стандартные Linux команды.
- **Перезагружайте Terminal:** Если процесс "залип", просто создайте новое окно терминала.

