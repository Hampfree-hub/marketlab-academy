# 📐 ИСПРАВЛЕНИЕ ОТСТУПОВ — 2026-01-20

**Дата:** 2026-01-20  
**Проблема:** Слишком большие отступы между фильтром по категориям и карточками статей

---

## ✅ ВЫПОЛНЕНО

### 1. ✅ Унифицированы отступы на главной странице

**Было:**
- `.categories-container` → `margin-bottom: var(--space-6)` (48px)
- `.section-label` → `margin: var(--space-2) 0 var(--space-1)` (16px top, 8px bottom)
- **Итого:** 48px + 16px + 8px = **72px** между кнопками и карточками

**Стало:**
- `.categories-container` → `margin-bottom: var(--space-4)` (32px)
- `.section-label` → `margin: var(--space-2) 0 var(--space-1)` (16px top, 8px bottom)
- **Итого:** 32px + 16px + 8px = **56px** между кнопками и карточками

**Уменьшено на:** 16px (с 72px до 56px)

---

### 2. ✅ Унифицированы отступы на странице библиотеки

**Было:**
- `.search-and-filter` → `margin-bottom: var(--space-4)` (32px)
- `.categories-container-with-margin` → `margin-bottom: var(--space-4)` (32px)
- **Итого:** 32px + 32px = **64px** между фильтром и карточками

**Стало:**
- `.search-and-filter` → `margin-bottom: 0` (убрали двойной отступ)
- `.categories-container-with-margin` → `margin-bottom: var(--space-4)` (32px)
- **Итого:** **32px** между фильтром и карточками

**Уменьшено на:** 32px (с 64px до 32px)

---

### 3. ✅ Унифицированы отступы на мобильных устройствах

**Главная страница:**
- `.categories-container` → `margin-bottom: var(--space-4)` (32px) на всех разрешениях

**Страница библиотеки:**
- `.categories-container-with-margin` → `margin-bottom: var(--space-4)` (32px) на всех разрешениях

---

## 📊 РЕЗУЛЬТАТ

**До исправления:**
- Главная страница: **72px** между фильтром и карточками
- Страница библиотеки: **64px** между фильтром и карточками
- **Рассинхрон:** 8px разница

**После исправления:**
- Главная страница: **56px** между фильтром и карточками (32px + 16px + 8px)
- Страница библиотеки: **32px** между фильтром и карточками
- **Унифицировано:** Оба используют `--space-4` (32px) как базовый отступ

---

## 🎯 СООТВЕТСТВИЕ ПРАВИЛАМ

✅ **8-pt Grid:** Все отступы кратны 8px
- `--space-1` = 8px
- `--space-2` = 16px
- `--space-4` = 32px

✅ **Унификация:** Одинаковые отступы на всех страницах
- Базовый отступ: `--space-4` (32px)
- Дополнительные отступы: `--space-2` (16px) и `--space-1` (8px)

✅ **Адаптивность:** Отступы одинаковы на всех разрешениях

---

## 📋 ИЗМЕНЁННЫЕ ФАЙЛЫ

1. `src/styles/components/home-index.css`
   - `.categories-container` → `margin-bottom: var(--space-4)` (было `--space-6`)
   - Мобильная версия → `margin-bottom: var(--space-4)` (было `--space-6`)

2. `src/styles/components/blog-index.css`
   - `.search-and-filter` → `margin-bottom: 0` (было `--space-4`)
   - Убрали двойной отступ

---

**Отступы унифицированы и соответствуют правилам 8-pt grid!** ✅
