# 📝 ПРОЦЕСС ПУБЛИКАЦИИ СТАТЕЙ — БЕЗОПАСНОСТЬ И РАБОТА

**Дата:** 2026-01-20

---

## ✅ КАК ЭТО РАБОТАЕТ СЕЙЧАС

### Автоматический деплой:

**Процесс:**
1. Создаёте статью в `src/content/blog-[lang]/[slug]/index.md`
2. Коммитите и пушите в ветку `main`
3. GitHub Actions автоматически:
   - Собирает проект (`npm run build`)
   - Деплоит на GitHub Pages
4. Статья сразу доступна на сайте

**Время:** Обычно 2-5 минут после push

---

## 🔒 БЕЗОПАСНОСТЬ

### ✅ Что безопасно:

**Автоматический деплой:**
- ✅ Сборка проверяет валидность контента
- ✅ Astro валидирует frontmatter через schema
- ✅ Ошибки сборки не дадут задеплоить битый код
- ✅ Можно откатить через git revert

**Проверка перед публикацией:**
- ✅ Локальная проверка: `npm run dev` (проверить статью локально)
- ✅ Локальная сборка: `npm run build` (убедиться, что собирается)
- ✅ Pre-commit hook: проверяет файлы перед коммитом

---

## 📋 РЕКОМЕНДУЕМЫЙ WORKFLOW

### Вариант 1: Прямой коммит в main (для проверенных статей)

**Когда использовать:**
- Статья полностью готова
- Проверена локально (`npm run dev`)
- Frontmatter корректен

**Шаги:**
```bash
# 1. Создать статью
# 2. Проверить локально
npm run dev

# 3. Проверить сборку
npm run build

# 4. Коммит и пуш
git add src/content/blog-ru/new-article/index.md
git commit -m "content: add 'Название статьи'"
git push origin main

# 5. GitHub Actions автоматически задеплоит
```

**Время до публикации:** 2-5 минут

---

### Вариант 2: Ветка для проверки (для новых/экспериментальных статей)

**Когда использовать:**
- Новая статья, нужно проверить
- Изменения в структуре
- Экспериментальный контент

**Шаги:**
```bash
# 1. Создать ветку
git checkout -b content/new-article

# 2. Добавить статью и проверить локально
npm run dev

# 3. Коммит
git add src/content/blog-ru/new-article/index.md
git commit -m "content: add 'Название статьи' (WIP)"

# 4. Push в ветку
git push origin content/new-article

# 5. Проверить на GitHub (если нужно)
# 6. Мержить в main когда готово
git checkout main
git merge content/new-article
git push origin main
```

**Время до публикации:** После мержа в main → 2-5 минут

---

## ⚠️ ЧТО ПРОВЕРЯТЬ ПЕРЕД ПУБЛЕМ

### Обязательные проверки:

1. **Локальная проверка:**
   ```bash
   npm run dev
   # Открыть http://localhost:4321/ru/library/new-article
   # Проверить отображение
   ```

2. **Проверка сборки:**
   ```bash
   npm run build
   # Убедиться, что нет ошибок
   ```

3. **Проверка frontmatter:**
   - ✅ Все обязательные поля заполнены
   - ✅ `category` правильная
   - ✅ `description` 150-160 символов
   - ✅ `pubDate` в формате YYYY-MM-DD

4. **Проверка контента:**
   - ✅ Нет местоимений "я", "мы", "вы"
   - ✅ Нет конфиденциальной информации
   - ✅ Все ссылки актуальны

---

## 🛡️ ЗАЩИТА ОТ ОШИБОК

### Pre-commit hook:

**Файл:** `scripts/git-commit.cjs` (если используется)

**Что проверяет:**
- Блокирует коммит конфиденциальных файлов
- Проверяет структуру перед коммитом

### GitHub Actions:

**Что проверяет:**
- Сборка проекта (`npm run build`)
- Если сборка падает → деплой не происходит
- Ошибки видны в логах Actions

### Откат изменений:

**Если что-то пошло не так:**
```bash
# Откатить последний коммит
git revert HEAD
git push origin main

# Или откатить к предыдущей версии
git reset --hard HEAD~1
git push origin main --force
```

---

## 📊 СТАТУС ДЕПЛОЯ

**Проверка статуса:**
- GitHub → Actions → Последний workflow run
- Зелёная галочка = успешно задеплоено
- Красный крестик = ошибка (нужно исправить)

**URL блога:**
- https://hampfree-hub.github.io/marketlab-academy

---

## ✅ ИТОГО

**Безопасно ли:**
- ✅ Да, если проверять локально перед пушем
- ✅ GitHub Actions не задеплоит битый код (сборка упадёт)
- ✅ Можно откатить изменения через git

**Рекомендация:**
- Для проверенных статей → прямой коммит в main
- Для новых/экспериментальных → ветка → проверка → мерж

**Время до публикации:** 2-5 минут после push в main

---

**Процесс безопасный и автоматизированный!** ✅
