# 🧪 КАК ПРОВЕРИТЬ OG ИЗОБРАЖЕНИЯ В TELEGRAM

**Дата:** 2026-01-20

---

## ✅ ШАГИ ДЛЯ ПРОВЕРКИ

### 1. Убедиться, что изображения сгенерированы

**Локально:**
```bash
npm run build
```

**Проверить:**
- В папке `dist/og/` должны появиться PNG файлы
- Формат: `{lang}-{slug}.png` (например, `ru-money-flow-index.png`)
- Должен быть файл `homepage.png` для главной

---

### 2. После деплоя на GitHub Pages

**Дождаться завершения деплоя:**
- GitHub Actions → Actions → Последний workflow run
- Должна быть зелёная галочка ✅

**URL блога:**
- https://hampfree-hub.github.io/marketlab-academy

---

### 3. Проверить в Telegram

**Вариант 1: Отправить ссылку себе**
1. Откройте Telegram
2. Отправьте себе ссылку на статью:
   - `https://hampfree-hub.github.io/marketlab-academy/ru/library/money-flow-index/`
3. Telegram автоматически покажет превью с OG изображением

**Вариант 2: Проверить через бота**
1. Найдите любого бота в Telegram
2. Отправьте ссылку на статью
3. Бот покажет превью

**Вариант 3: Проверить напрямую**
1. Откройте в браузере:
   - `https://hampfree-hub.github.io/marketlab-academy/og/ru-money-flow-index.png`
2. Должно открыться изображение 1200x630px

---

### 4. Проверить мета-теги

**В браузере:**
1. Откройте любую статью
2. Откройте DevTools (F12)
3. Перейдите в Elements/Inspector
4. Найдите `<meta property="og:image">`
5. URL должен быть: `/marketlab-academy/og/{lang}-{slug}.png`

**Или через консоль:**
```javascript
document.querySelector('meta[property="og:image"]')?.content
```

---

## 🔍 ЧТО ПРОВЕРЯТЬ

### ✅ Успешная генерация:
- В `dist/og/` есть PNG файлы
- Размер изображений: 1200x630px
- Стиль соответствует Banner.astro (8-bit NES)
- Категории и цвета правильные

### ✅ Правильные мета-теги:
- `<meta property="og:image">` указывает на `/marketlab-academy/og/...`
- URL полный и корректный
- Изображение доступно по URL

### ✅ Превью в Telegram:
- Показывается кастомное изображение (не Astro placeholder)
- Изображение соответствует дизайну блога
- Текст читаемый (заголовок, описание, категория)

---

## 🐛 ЕСЛИ НЕ РАБОТАЕТ

### Проблема: Изображения не генерируются

**Проверить:**
1. Запустить скрипт вручную: `npm run generate-og`
2. Проверить ошибки в консоли
3. Убедиться, что `dist/` существует (после `npm run build`)

### Проблема: Telegram показывает старое превью

**Решение:**
1. Telegram кэширует превью
2. Использовать инструмент для очистки кэша:
   - https://developers.facebook.com/tools/debug/
   - Вставить URL статьи → "Scrape Again"
3. Или подождать несколько часов (кэш обновится автоматически)

### Проблема: Изображение не загружается

**Проверить:**
1. URL изображения доступен: `https://hampfree-hub.github.io/marketlab-academy/og/...`
2. Файл существует в `dist/og/` после сборки
3. GitHub Pages правильно деплоит файлы из `dist/`

---

## 📋 ЧЕКЛИСТ ПРОВЕРКИ

- [ ] `npm run build` выполняется без ошибок
- [ ] В `dist/og/` есть PNG файлы
- [ ] Изображения открываются в браузере
- [ ] Мета-теги правильные в HTML
- [ ] Telegram показывает кастомное превью (после очистки кэша)
- [ ] Главная страница имеет OG изображение

---

**После деплоя проверьте в Telegram!** ✅
