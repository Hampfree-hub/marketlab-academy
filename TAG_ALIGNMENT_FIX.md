# ✅ ИСПРАВЛЕНО ВЫРАВНИВАНИЕ ТЕГОВ НА КАРТОЧКАХ — 2026-01-20

**Дата:** 2026-01-20  
**Статус:** ✅ Выполнено

---

## 🎯 ПРОБЛЕМА

Теги (категории) на карточках статей находились на разных вертикальных уровнях из-за разной длины описаний:
- Описание в 3 строки → тег ниже
- Описание в 2 строки → тег выше

Это создавало визуальную несогласованность в сетке карточек.

---

## ✅ РЕШЕНИЕ

### 1. **Фиксированная минимальная высота для subtitle**
- Добавлен `min-height` для `.banner-subtitle` в preview баннерах
- Высота рассчитывается на основе максимального количества строк (3 строки)
- Разные значения для разных размеров текста (text-xs, text-sm, text-base)

### 2. **Фиксированная минимальная высота для области title+subtitle**
- Добавлен `min-height` для `.banner-top` в preview баннерах
- Гарантирует, что область с заголовком и описанием всегда занимает одинаковое пространство
- Теги всегда выравниваются на одном уровне

---

## 📋 ИЗМЕНЕНИЯ В КОДЕ

**Файл:** `src/components/Banner.astro`

**Добавлено:**
```css
/* Фиксируем минимальную высоту для subtitle */
.banner-preview .banner-subtitle {
    -webkit-line-clamp: 3;
    min-height: calc(3 * 1.5em); /* 3 строки × line-height 1.5 */
}

.banner-preview .banner-subtitle.text-xs { 
    min-height: calc(4 * 1.5em); /* 4 строки для text-xs */
}
.banner-preview .banner-subtitle.text-sm { 
    min-height: calc(3 * 1.5em); /* 3 строки для text-sm */
}
.banner-preview .banner-subtitle.text-base { 
    min-height: calc(2 * 1.5em); /* 2 строки для text-base */
}

/* Фиксируем минимальную высоту для области title+subtitle */
.banner-preview .banner-top {
    min-height: calc(1.6em + 3 * 1.5em + var(--space-1)); 
    display: flex;
    flex-direction: column;
}
```

---

## 🎯 РЕЗУЛЬТАТ

Теперь теги выравниваются на одном уровне:
- ✅ Независимо от длины описания (2 или 3 строки)
- ✅ На всех разрешениях экрана
- ✅ Соответствует 8-pt grid system (используются CSS переменные)

---

## 📱 АДАПТИВНОСТЬ

Изменения работают на всех разрешениях:
- ✅ Desktop (2 колонки)
- ✅ Tablet (1 колонка)
- ✅ Mobile (1 колонка)

Минимальная высота гарантирует единообразие на всех устройствах.

---

**Выравнивание тегов унифицировано!** ✅
