# ✅ УДАЛЁН bannerImage ИЗ FRONTMATTER — 2026-01-20

**Дата:** 2026-01-20  
**Статус:** ✅ Выполнено

---

## 🎯 ПРОБЛЕМА

В frontmatter всех статей было указано `bannerImage: './assets/banner.png'`, хотя баннеры генерируются **автоматически** через компонент `<Banner>` (CSS/HTML, не изображение).

---

## ✅ РЕШЕНИЕ

Удалён `bannerImage` из frontmatter **всех 24 статей**:
- ✅ 8 русских статей (blog-ru)
- ✅ 8 английских статей (blog-en)
- ✅ 8 испанских статей (blog-es)

---

## 📋 КАК РАБОТАЕТ СИСТЕМА БАННЕРОВ

### 1. **Баннер в статье** (генерируется автоматически)
- Компонент `<Banner>` в `src/components/Banner.astro`
- Генерируется динамически через CSS/HTML
- Использует `title`, `description`, `category` из frontmatter
- **Не требует** `bannerImage` в frontmatter

### 2. **OG изображения** (для соцсетей)
- Используется в `<BaseHead>` для Open Graph мета-тегов
- Если `bannerImage` не указан → используется fallback: `blog-placeholder-1.jpg`
- Это нормально, так как баннеры генерируются автоматически

---

## 🔄 ИЗМЕНЕНИЯ

**До:**
```markdown
---
title: 'Article Title'
description: 'Description'
category: 'crypto'
bannerImage: './assets/banner.png'
---
```

**После:**
```markdown
---
title: 'Article Title'
description: 'Description'
category: 'crypto'
---
```

---

## 📝 ПРИМЕЧАНИЕ

- `bannerImage` остаётся **опциональным** в `content.config.ts` (на случай, если понадобится указать OG изображение вручную)
- Для OG изображений используется fallback, что нормально
- В будущем можно добавить автоматическую генерацию OG изображений на основе компонента `<Banner>`

---

**Всё готово! Баннеры генерируются автоматически, `bannerImage` больше не нужен в frontmatter.** ✅
