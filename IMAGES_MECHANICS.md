# 🖼️ Механика изображений в блоге

**Дата:** 20 января 2026  
**Статус:** Актуально

---

## 📊 ТЕКУЩАЯ МЕХАНИКА

### Превью статей в списке (карточки):

**Используется:** Компонент `<Banner>` с `type="preview"`
- ✅ CSS-баннеры, генерируются автоматически
- ✅ Паттерны в зависимости от категории
- ✅ 8-bit стиль с эффектами
- **Где:** `src/pages/[lang]/library/index.astro`, `src/pages/[lang]/index.astro`

**НЕ используется:** `previewImage` из frontmatter
- ❌ Поле есть в схеме, но не используется в компонентах
- ❌ Указан только в `bitcoin-basics`, но нигде не обрабатывается

### Баннеры в статьях:

**Используется:** Компонент `<Banner>` с `type="full"`
- ✅ CSS-баннеры, генерируются автоматически
- ✅ Полный баннер в начале статьи
- **Где:** `src/layouts/BlogPost.astro`

**НЕ используется:** `bannerImage`, `heroImage` из frontmatter
- ❌ Поля есть в схеме (`heroImage`, `bannerImage`)
- ❌ Используются только для передачи в `BaseHead`, но не для отображения в статье
- ⚠️ В `BaseHead` используется `displayImage = heroImage || bannerImage`, но передаётся в `<Banner>`, а не как изображение

### OG изображения (для Telegram и мессенджеров):

**Используется:** Автоматически сгенерированные PNG изображения
- ✅ Генерируются при сборке (`npm run build`)
- ✅ Формат: PNG, 1200x630px
- ✅ Сохраняются в `dist/og/`
- ✅ Формат имени: `/og/{lang}-{slug}.png`
- **Где:** `src/integrations/og-images.ts`, `scripts/generate-og-images.mjs`
- **Механика:** При отсутствии явного изображения в BaseHead, автоматически определяется путь к сгенерированному OG изображению

**Приоритет в BaseHead:**
1. Явно указанное изображение (`imageProp`)
2. Автоматически сгенерированное OG изображение (`/og/{lang}-{slug}.png`)
3. Fallback изображение (`blog-placeholder-1.jpg`)

---

## 🔍 АНАЛИЗ ИСПОЛЬЗОВАНИЯ ПОЛЕЙ

### `previewImage`:
- ✅ Есть в схеме (`content.config.ts`)
- ❌ НЕ используется в компонентах
- ⚠️ Указан только в `bitcoin-basics/index.md`: `previewImage: './assets/banner.png'`
- ❌ Не отображается нигде

### `bannerImage`:
- ✅ Есть в схеме
- ✅ Используется в `BlogPost.astro` для передачи в `BaseHead`
- ❌ НЕ используется для отображения баннера в статье (используется компонент `<Banner>`)

### `heroImage`:
- ✅ Есть в схеме
- ✅ Используется в `BlogPost.astro` для передачи в `BaseHead`
- ❌ НЕ используется для отображения в статье (используется компонент `<Banner>`)

---

## ✅ ВЫВОДЫ

1. **Превью статей в списке:**
   - Используются CSS-баннеры через компонент `<Banner type="preview">`
   - `previewImage` НЕ используется

2. **Баннеры в статьях:**
   - Используются CSS-баннеры через компонент `<Banner type="full">`
   - `bannerImage`, `heroImage` НЕ используются для отображения

3. **OG изображения для Telegram и мессенджеров:**
   - Автоматически генерируются PNG изображения (1200x630px)
   - Используются при шеринге в соцсетях и мессенджерах
   - **Это то, что видно при пересылке ссылки в Telegram**

---

## 📝 РЕКОМЕНДАЦИИ

1. **Убрать `previewImage` из `bitcoin-basics`:**
   - Он не используется нигде
   - Превью генерируются автоматически через CSS

2. **Оставить поля `bannerImage` и `heroImage`:**
   - Они опциональны
   - Могут использоваться в будущем для кастомных изображений

3. **Текущая механика работает отлично:**
   - CSS-баннеры для превью и баннеров в статьях
   - Автоматическая генерация OG изображений для мессенджеров
   - Нет необходимости в статических previewImage

---

## 🎨 WebP ФОРМАТ

### Текущее состояние:

**OG изображения:** PNG формат (1200x630px)
- Генерируются автоматически при сборке
- Используются для Telegram и мессенджеров
- Размер файла зависит от контента (обычно 50-200KB)

**Изображения в статьях:**
- В статьях используются встроенные изображения (если есть)
- Формат зависит от исходного файла (обычно JPG/PNG)

### Мнение о WebP:

✅ **WebP — отличный формат для изображений в статьях:**
- Меньший размер файла (на 40-60%)
- Быстрая загрузка страниц
- Лучше для SEO (PageSpeed Insights)
- Современная поддержка браузерами (95%+)

⚠️ **НО для OG изображений WebP не подойдёт:**
- Telegram и многие мессенджеры ожидают PNG/JPG для превью ссылок
- WebP может не поддерживаться для Open Graph
- **Рекомендация:** Оставляем PNG для OG изображений

✅ **Рекомендация:**
- **OG изображения:** Оставляем PNG (как сейчас)
- **Изображения в статьях:** Использовать WebP (если добавляем новые изображения)
- **Конвертация:** Использовать Squoosh.app для оптимизации перед загрузкой

---

**Вывод:** Текущая механика превью работает через CSS-баннеры, OG изображения генерируются автоматически. `previewImage` не используется и может быть удалён из статей.
