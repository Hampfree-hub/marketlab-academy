# 🔧 Исправление ошибок 404 для шрифтов

**Проблема:** Шрифты не загружаются (404 Not Found)

---

## 🔍 Что видно в DevTools:

**Ошибки:**
```
GET http://localhost:4321/fonts/atkinson-regular.woff 404 (Not Found)
GET http://localhost:4321/fonts/atkinson-bold.woff 404 (Not Found)
```

**Предупреждения:**
- Шрифты preload, но не используются

---

## 🔍 Причина:

**Шрифты находятся в:**
```
public/fonts/atkinson-regular.woff
public/fonts/atkinson-bold.woff
```

**Путь в CSS:**
```css
src: url("/fonts/atkinson-regular.woff")
```

**Проблема:** В dev-режиме Astro может неправильно обрабатывать пути из `public/`.

---

## ✅ Решение:

### Вариант 1: Исправить путь в CSS (рекомендую)

**В `src/styles/global.css`:**
```css
@font-face {
	font-family: "Atkinson";
	src: url("/marketlab-blog/fonts/atkinson-regular.woff") format("woff");
	/* ... */
}
```

**Но это не правильно!** Нужно использовать правильный путь.

---

### Вариант 2: Использовать правильный путь для base

**В `astro.config.mjs` base уже настроен:**
```javascript
base: '/marketlab-blog',
```

**Проблема:** В dev-режиме base может не применяться к `public/`.

---

### Вариант 3: Проверить что файлы на месте

**Файлы есть:**
```
public/fonts/atkinson-regular.woff ✅
public/fonts/atkinson-bold.woff ✅
```

**Значит проблема в пути или настройках Astro.**

---

## 🔧 Исправление:

**Проверю и исправлю пути в CSS и preload ссылках.**

---

**Исправлю сейчас!** 🔧

