# 🌐 Настройка кастомного домена для MarketLab Academy

## 📋 Что нужно сделать

### **Шаг 1: Настроить домен в GitHub**

1. Откройте репозиторий: https://github.com/Hampfree-hub/marketlab-academy
2. Перейдите в **Settings → Pages**
3. В разделе **Custom domain** введите ваш домен
4. Нажмите **Save**

GitHub автоматически создаст файл `CNAME` в репозитории.

---

### **Шаг 2: Настроить DNS записи**

У вашего регистратора домена (где вы купили домен) нужно добавить DNS записи:

#### **Вариант A: Корневой домен (example.com)**

**Для корневого домена:**
- **Тип:** `A` (4 записи)
- **Имя:** `@` или пусто
- **Значение:** 
  - `185.199.108.153`
  - `185.199.109.153`
  - `185.199.110.153`
  - `185.199.111.153`

**Для www поддомена:**
- **Тип:** `CNAME`
- **Имя:** `www`
- **Значение:** `hampfree-hub.github.io`

#### **Вариант B: Поддомен (blog.example.com)**

- **Тип:** `CNAME`
- **Имя:** `blog` (или любое другое)
- **Значение:** `hampfree-hub.github.io`

---

### **Шаг 3: Обновить astro.config.mjs**

После настройки DNS (может занять до 24 часов) обновите конфигурацию:

```javascript
export default defineConfig({
	site: 'https://ВАШ_ДОМЕН.com',  // Ваш кастомный домен
	base: '/',  // Убрать base, если домен корневой
	// или
	base: '/marketlab-academy',  // Оставить, если поддомен
	integrations: [mdx(), sitemap()],
});
```

**Важно:** 
- Если домен корневой (`example.com`) → `base: '/'`
- Если поддомен (`blog.example.com`) → можно оставить `base: '/marketlab-academy'` или убрать

---

### **Шаг 4: Обновить BaseHead.astro (если нужно)**

Если изменился `base`, проверьте пути к OG изображениям в `BaseHead.astro`:

```typescript
// Если base: '/', то путь будет:
ogImageUrl = new URL(`/og/${lang}-${slug}.png`, Astro.site);

// Если base: '/marketlab-academy', то путь будет:
ogImageUrl = new URL(`/marketlab-academy/og/${lang}-${slug}.png`, Astro.site);
```

---

### **Шаг 5: Проверить**

1. **Дождаться DNS распространения** (обычно 1-24 часа)
2. **Проверить доступность:**
   ```
   https://ВАШ_ДОМЕН.com
   ```
3. **Проверить OG изображения:**
   ```
   https://ВАШ_ДОМЕН.com/og/ru-bitcoin-basics.png
   ```
4. **Проверить в Telegram:**
   - Отправить ссылку на статью
   - Убедиться, что превью работает

---

## 🔍 Проверка DNS

Проверить, что DNS записи настроены правильно:

```bash
# Windows
nslookup ВАШ_ДОМЕН.com

# Или онлайн:
# https://dnschecker.org/
```

---

## ⚠️ Важные замечания

1. **DNS распространение:** Может занять до 24 часов
2. **HTTPS:** GitHub автоматически выдаёт SSL сертификат (может занять несколько часов)
3. **CNAME файл:** GitHub создаст его автоматически, не удаляйте его
4. **Проверка домена:** GitHub может попросить подтвердить владение доменом

---

## 📝 Примеры конфигурации

### **Корневой домен (marketlab.academy):**

```javascript
// astro.config.mjs
site: 'https://marketlab.academy',
base: '/',
```

### **Поддомен (blog.marketlab.academy):**

```javascript
// astro.config.mjs
site: 'https://blog.marketlab.academy',
base: '/',
```

---

## ✅ Чеклист

- [ ] Домен добавлен в GitHub Pages Settings
- [ ] DNS записи настроены у регистратора
- [ ] DNS проверен (nslookup или dnschecker.org)
- [ ] `astro.config.mjs` обновлён с новым доменом
- [ ] `base` настроен правильно
- [ ] Сайт доступен по новому домену
- [ ] OG изображения доступны
- [ ] Telegram показывает превью

---

**После настройки домена обновите документацию с новым URL!**
