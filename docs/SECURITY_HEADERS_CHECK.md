# 🔒 Проверка Security Headers — пошаговая инструкция

**Дата:** 12.12.2025  
**Цель:** Проверить что Security Headers работают

---

## 📋 Шаг 1: Проверить Security Headers в dev-режиме

### Что проверить:

**Security Headers которые должны быть:**
1. ✅ `X-Frame-Options: DENY` (защита от clickjacking)
2. ✅ `X-Content-Type-Options: nosniff` (защита от MIME-sniffing)
3. ✅ `Referrer-Policy: strict-origin-when-cross-origin` (контроль приватности)
4. ✅ `Strict-Transport-Security` (HSTS)
5. ✅ `Content-Security-Policy` (CSP)

---

### Как проверить:

#### Способ 1: Через DevTools (рекомендую)

1. **Откройте блог в браузере:**
   ```
   http://localhost:4321/marketlab-blog/
   ```

2. **Откройте DevTools:**
   - Нажмите `F12` или `Ctrl+Shift+I`
   - Или правый клик → "Inspect" (Проверить элемент)

3. **Перейдите на вкладку "Network" (Сеть):**
   - Нажмите `F5` (обновить страницу)
   - Выберите первый запрос (обычно `index` или название страницы)

4. **Посмотрите Headers (Заголовки):**
   - Вкладка "Headers"
   - Прокрутите до "Response Headers" (Заголовки ответа)
   - Ищите Security Headers

**Что должно быть:**
```
X-Frame-Options: DENY
X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
Content-Security-Policy: default-src 'self'; ...
```

---

#### Способ 2: Через curl (в терминале)

```powershell
curl -I http://localhost:4321/marketlab-blog/
```

**Что искать:**
- Те же заголовки в выводе

---

### Если Headers есть ✅:

**Отлично!** Security Headers работают в dev-режиме.

**Что дальше:**
- Переходим к проверке HTTPS на GitHub Pages

---

### Если Headers нет ❌:

**Проблема:** Middleware не работает.

**Что делать:**
1. Проверить что `src/middleware.ts` существует
2. Проверить что middleware правильно настроен
3. Перезапустить dev-сервер

---

## 📋 Шаг 2: Проверить HTTPS на GitHub Pages

### Что проверить:

1. **HTTPS работает:**
   - ✅ URL начинается с `https://`
   - ✅ Замочек 🔒 в адресной строке
   - ✅ Нет предупреждений

2. **Сертификат валидный:**
   - ✅ Нажать на замочек
   - ✅ Должно быть: "Connection is secure"
   - ✅ Сертификат: "GitHub, Inc."

---

### Как проверить:

1. **Откройте блог онлайн:**
   ```
   https://hampfree-hub.github.io/marketlab-blog/
   ```

2. **Проверьте адресную строку:**
   - ✅ Должен быть замочек 🔒
   - ✅ URL должен начинаться с `https://`

3. **Нажмите на замочек:**
   - Должно быть: "Connection is secure"
   - Сертификат: "GitHub, Inc."

---

### Если HTTPS работает ✅:

**Отлично!** GitHub Pages автоматически настроил HTTPS.

**Что дальше:**
- Переходим к проверке Security Headers на GitHub Pages (если нужен адаптер)

---

### Если HTTPS не работает ❌:

**Проблема:** Сертификат не настроен.

**Что делать:**
1. Проверить настройки GitHub Pages
2. Подождать несколько минут (сертификат может обновляться)
3. Проверить что репозиторий публичный (для бесплатного HTTPS)

---

## 📋 Шаг 3: Проверить Security Headers на GitHub Pages

### ⚠️ Важное замечание:

**Middleware работает:**
- ✅ В dev-режиме (`npm run dev`) — работает
- ✅ При использовании адаптера — работает

**Middleware НЕ работает:**
- ❌ На GitHub Pages напрямую (GitHub Pages раздаёт статические файлы)
- ❌ Без адаптера для production

**Вывод:**
- Security Headers работают в dev-режиме ✅
- Для production нужен адаптер (можно позже)

---

## ✅ Итоговый чеклист:

### Dev-режим:
- [ ] Dev-сервер запущен
- [ ] Блог открыт: `http://localhost:4321/marketlab-blog/`
- [ ] DevTools открыты (F12)
- [ ] Вкладка "Network" открыта
- [ ] Security Headers видны в Response Headers

### GitHub Pages:
- [ ] Блог открыт: `https://hampfree-hub.github.io/marketlab-blog/`
- [ ] HTTPS работает (замочек)
- [ ] Сертификат валидный

---

## 🚀 Что дальше:

После проверки:
1. Если всё ок → продолжаем работу по безопасности
2. Если есть проблемы → исправлю

---

**Проверьте и скажите что видите!** 🔍

