# 🔧 PowerShell: Лучшие практики для AI-ассистентов

**Дата:** 22 января 2026  
**Статус:** ✅ Актуально

---

## ⚠️ КРИТИЧЕСКИ ВАЖНО

**Команды с несколькими операциями в одной строке могут зависать в PowerShell при работе через AI-ассистентов!**

---

## 🚫 ЧТО НЕ ДЕЛАТЬ

### ❌ НЕ объединяйте несколько операций в одну строку:

```powershell
# НЕПРАВИЛЬНО — может зависнуть!
cd d:\Projects\HampfreeBlog; if (Test-Path "docs\ARTICLES_WORK_PLAN.md") { Move-Item -Path "docs\ARTICLES_WORK_PLAN.md" -Destination "..\HampfreeBlog-Private\docs\" -Force; Write-Host "Moved" }; git status
```

**Проблемы:**
- Команда может зависнуть без видимой причины
- Сложно отладить, где именно произошла ошибка
- AI-ассистент не может корректно обработать сложную цепочку

---

## ✅ ПРАВИЛЬНЫЙ ПОДХОД

### ✅ Выполняйте команды по отдельности:

```powershell
# Команда 1: Переход в директорию
cd d:\Projects\HampfreeBlog

# Команда 2: Проверка существования файла и перемещение
if (Test-Path "docs\ARTICLES_WORK_PLAN.md") { 
    Move-Item -Path "docs\ARTICLES_WORK_PLAN.md" -Destination "..\HampfreeBlog-Private\docs\" -Force 
}

# Команда 3: Вывод сообщения
Write-Host "Moved ARTICLES_WORK_PLAN.md to private repo"

# Команда 4: Проверка статуса Git
git status
```

**Преимущества:**
- ✅ Команды выполняются стабильно
- ✅ Легко отладить, если что-то пошло не так
- ✅ AI-ассистент корректно обрабатывает каждую команду
- ✅ Нет зависаний

---

## 📋 ПРАВИЛА

### 1. Разделяйте команды

**Правило:** Одна команда = один вызов `run_terminal_cmd`.

**Неправильно:**
```powershell
cd D:\Projects; git status; git add .; git commit -m "fix"
```

**Правильно:**
```powershell
# Команда 1
cd D:\Projects

# Команда 2
git status

# Команда 3
git add .

# Команда 4
git commit -m "fix"
```

### 2. Упрощайте условия

**Правило:** Разбивайте сложные условия на отдельные команды.

**Неправильно:**
```powershell
if (Test-Path "file.md") { Move-Item "file.md" "dest\"; Write-Host "Done" }; git status
```

**Правильно:**
```powershell
# Команда 1: Проверка и перемещение
if (Test-Path "file.md") { 
    Move-Item "file.md" "dest\" 
}

# Команда 2: Вывод сообщения
Write-Host "Done"

# Команда 3: Статус Git
git status
```

### 3. Используйте `;` только для простых команд

**Правило:** `;` можно использовать только для очень простых команд без условий.

**Допустимо (но не рекомендуется):**
```powershell
cd D:\Projects; git status
```

**Лучше:**
```powershell
cd D:\Projects
git status
```

### 4. НИКОГДА не используйте `&&`

**Правило:** В PowerShell `&&` не работает так же, как в bash.

**Неправильно:**
```powershell
cd D:\Projects && git status  # ❌ ОШИБКА!
```

**Правильно:**
```powershell
cd D:\Projects
git status
```

---

## 🔍 ПРИЗНАКИ ЗАВИСАНИЯ

Если команда:
- ⏱️ Выполняется дольше 30 секунд без вывода
- 🔄 Не возвращает результат
- ⚠️ Показывает "Command failed to spawn: Aborted"

**Решение:** Разбейте команду на отдельные шаги!

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- `D:\Projects\AutomationHub\ContentBot\WORKFLOW_RULES.md` — общие правила работы
- `D:\Projects\protocol-sherlock.md` — протокол аудита (раздел 3.0.1)

---

## ✅ ЧЕКЛИСТ

Перед выполнением команды проверьте:

- [ ] Команда содержит только одну операцию?
- [ ] Нет сложных условий в одной строке?
- [ ] Нет использования `&&`?
- [ ] Команда разбита на логические шаги?

**Если хотя бы один пункт "нет" — разбейте команду на части!**

---

**Последнее обновление:** 22 января 2026
