# 📱 OG изображения для Telegram

## ✅ Как это работает

При отправке ссылки на статью в Telegram, автоматически подтягивается OG изображение из meta tags.

### **Механика:**

1. **Генерация изображений:**
   - При сборке (`npm run build`) автоматически генерируются OG изображения
   - Сохраняются в `dist/og/` с именами `{lang}-{slug}.png`
   - Используют те же стили, что и баннеры на сайте

2. **Meta tags:**
   - `BaseHead.astro` автоматически добавляет `og:image` и `twitter:image`
   - Путь: `/marketlab-academy/og/{lang}-{slug}.png`
   - Telegram читает эти meta tags и показывает превью

3. **Деплой:**
   - Изображения из `dist/og/` автоматически деплоятся на GitHub Pages
   - Доступны по URL: `https://hampfree-hub.github.io/marketlab-academy/og/{lang}-{slug}.png`

---

## 🔍 Проверка

### **Локально:**
```bash
npm run build
npm run preview
# Откройте http://localhost:4321/og/ru-bitcoin-basics.png
```

### **В браузере (после деплоя):**
```
https://hampfree-hub.github.io/marketlab-academy/og/ru-bitcoin-basics.png
```

### **Проверка meta tags:**
1. Откройте статью на сайте
2. Посмотрите исходный код страницы (Ctrl+U)
3. Найдите `<meta property="og:image" content="...">`
4. Скопируйте URL и откройте в браузере

---

## 📋 Формат изображений

- **Размер:** 1200×630px (стандарт OG изображений)
- **Формат:** PNG
- **Стиль:** 8-bit NES (как баннеры на сайте)
- **Шрифты:** Press Start 2P (заголовки), JetBrains Mono (описание)

---

## 🎨 Категории и переводы

Категории автоматически переводятся на язык статьи:

| Категория | Русский | English | Español |
|-----------|---------|---------|---------|
| `crypto` | КРИПТО | CRYPTO | CRIPTO |
| `technical-analysis` | ТЕХАНАЛИЗ | TECHNICAL | TÉCNICO |
| `algo-trading` | АЛГО | ALGO | ALGO |
| `fundamental-analysis` | ФУНДАМЕНТАЛЬНЫЙ | FUNDAMENTAL | FUNDAMENTAL |
| `general` | ОБЩЕЕ | GENERAL | GENERAL |

---

## 🚀 Что делать, если изображение не подтягивается?

1. **Проверьте, что изображение сгенерировано:**
   ```bash
   ls dist/og/ru-{slug}.png
   ```

2. **Проверьте meta tags:**
   - Откройте статью на сайте
   - Посмотрите исходный код
   - Найдите `og:image` - URL должен быть правильным

3. **Очистите кеш Telegram:**
   - Telegram кеширует превью
   - Попробуйте отправить ссылку через [Telegram Bot API](https://core.telegram.org/bots/api#getlinkpreview) для проверки

4. **Проверьте URL изображения:**
   - URL должен быть абсолютным (начинаться с `https://`)
   - Изображение должно быть доступно публично

---

## ✅ Итог

- ✅ Изображения генерируются автоматически
- ✅ Meta tags добавляются автоматически
- ✅ Telegram подтягивает превью из `og:image`
- ✅ Всё работает "из коробки"

**Просто отправьте ссылку на статью в Telegram — превью появится автоматически!** 🎉
