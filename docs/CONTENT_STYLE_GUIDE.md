# 📐 ЕДИНАЯ ИНСТРУКЦИЯ ПО ОФОРМЛЕНИЮ КОНТЕНТА

**Дата создания:** 2026-01-20  
**Статус:** Актуально  
**Версия:** 1.0

> **Железобетонная инструкция** — единая точка опоры для оформления контента в MarketLab Academy. Следуя этим правилам, вы гарантируете, что ничего не сломается и всё будет красиво.

---

## 🎯 ФИЛОСОФИЯ

**70% серьёзное / 30% забавное**

Блог использует 8-bit NES стиль с акцентом на читаемость и профессиональность. Все элементы должны соответствовать дизайн-системе.

---

## 📐 СИСТЕМА ОТСТУПОВ (8-pt Grid)

### Базовый принцип

**ВСЁ кратно 8px** — это основа аутентичного 8-bit NES стиля.

### Доступные переменные

```css
--space-0: 0;
--space-1: 8px;    /* 1 × 8 — минимальный gap (иконки) */
--space-2: 16px;   /* 2 × 8 — стандартный padding (кнопки, элементы) */
--space-3: 24px;   /* 3 × 8 — gap между элементами в ряду */
--space-4: 32px;   /* 4 × 8 — margin между компонентами */
--space-5: 40px;   /* 5 × 8 — промежуточный размер */
--space-6: 48px;   /* 6 × 8 — margin между секциями */
--space-8: 64px;   /* 8 × 8 — padding крупных секций (hero) */
--space-10: 80px;  /* 10 × 8 — padding топ-секций (hero top) */
--space-12: 96px;  /* 12 × 8 — margin для крупных секций */
```

### ❌ ЧТО НЕЛЬЗЯ

```css
/* ❌ НЕПРАВИЛЬНО — не кратно 8px */
padding: 12px;
margin: 10px;
gap: 15px;

/* ❌ НЕПРАВИЛЬНО — хардкод значения */
padding-left: 20px;
margin-top: 25px;
```

### ✅ ЧТО НУЖНО

```css
/* ✅ ПРАВИЛЬНО — используем CSS переменные */
padding: var(--space-2);        /* 16px */
margin: var(--space-4);         /* 32px */
gap: var(--space-3);            /* 24px */

/* ✅ ПРАВИЛЬНО — если нужно нестандартное значение, кратно 8px */
padding-left: 24px;             /* 3 × 8 */
margin-top: 40px;               /* 5 × 8 */
```

---

## 📝 ФОРМАТИРОВАНИЕ КОНТЕНТА

### Структура заголовков

**Правильная иерархия:**
```markdown
# H1 — заголовок статьи (один на страницу, автоматически)

## H2 — основные разделы
### H3 — подразделы
#### H4 — детализация (использовать редко)
```

**Автоматическое форматирование:**
- Заголовки 2-5 слов → `.brutal` (CAPS, большой трекинг)
- Заголовки 5+ слов → `.long` (обычный вид, без CAPS)

**Правила:**
- ✅ Один H1 на страницу (генерируется автоматически из frontmatter)
- ✅ Используйте H2 для основных разделов
- ✅ Используйте H3 для подразделов
- ⚠️ H4 использовать редко (только для детализации)

### Абзацы

**Правила:**
- ✅ Короткие абзацы (2-3 предложения)
- ✅ Один абзац = одна мысль
- ✅ Между абзацами автоматически добавляется `var(--space-4)` (32px)

**Пример:**
```markdown
Биткоин — это первая в мире криптовалюта, основанная на технологии блокчейн.

Основной идеей системы является децентрализация: отсутствие единого управляющего органа.
```

### Списки

**Маркированные списки:**
```markdown
- Элемент 1
- Элемент 2
- Элемент 3
```

**Нумерованные списки:**
```markdown
1. Первый пункт
2. Второй пункт
3. Третий пункт
```

**Правила:**
- ✅ Используйте списки для перечисления
- ✅ Один элемент = одна строка
- ✅ Между списками автоматически добавляется `var(--space-6)` (48px)

### Выделение текста

**Жирный текст:**
```markdown
**Важный термин** или **ключевая мысль**
```

**Курсив:**
```markdown
*Акцент* или *название*
```

**Код:**
```markdown
`переменная` или `функция()`
```

**Правила:**
- ✅ Используйте **жирный** для ключевых терминов
- ✅ Используйте `код` для технических терминов
- ⚠️ Не злоупотребляйте выделением

---

## 🎨 ВИЗУАЛЬНЫЕ ЭЛЕМЕНТЫ

### Изображения

**Формат:**
```markdown
![Описание изображения](./assets/image.png)
```

**Правила:**
- ✅ Все изображения в папке `assets/` внутри статьи
- ✅ Используйте относительные пути: `./assets/image.png`
- ✅ Добавляйте alt-текст для доступности
- ✅ Размер баннера: 1200x630px PNG

### Блоки кода

**Инлайн код:**
```markdown
Используйте `переменная` в тексте.
```

**Блок кода:**
````markdown
```javascript
const example = "код";
```
````

**Правила:**
- ✅ Используйте инлайн код для переменных и функций
- ✅ Используйте блоки кода для примеров
- ✅ Указывайте язык для подсветки синтаксиса

### Цитаты

**Формат:**
```markdown
> Это важная цитата или выделение ключевой мысли.
```

**Правила:**
- ✅ Используйте для выделения важных мыслей
- ✅ Не злоупотребляйте цитатами

---

## 📋 FRONTMATTER

### Обязательные поля

```yaml
---
title: 'Заголовок статьи'
description: 'SEO описание 150-160 символов'
pubDate: '2026-01-17'
category: 'technical-analysis'
---
```

**Правила:**
- ✅ `title` — заголовок статьи (до 60 символов для SEO)
- ✅ `description` — SEO описание (150-160 символов)
- ✅ `pubDate` — дата публикации (YYYY-MM-DD, с кавычками)
- ✅ `category` — одна из 4 категорий

### Опциональные поля

```yaml
---
updatedDate: '2026-01-18'
bannerImage: './assets/banner.png'
tags: ['rsi', 'technical-analysis']
pinned: true
---
```

**Правила:**
- ✅ `updatedDate` — дата обновления (если статья обновлялась)
- ✅ `bannerImage` — путь к баннеру (относительный)
- ✅ `tags` — массив тегов (1-3 тега, релевантные)
- ✅ `pinned` — закрепить статью на главной (true/false)

### Категории

**Доступные категории:**
- `technical-analysis` — технический анализ
- `crypto` — криптовалюта
- `algo-trading` — алготрейдинг
- `fundamental-analysis` — фундаментальный анализ

**Правила:**
- ✅ Одна категория на статью
- ✅ Выбирайте категорию по содержанию, а не по заголовку

---

## 🐛 ЧАСТЫЕ ОШИБКИ И КАК ИХ ИЗБЕЖАТЬ

### ❌ Ошибка 1: Неправильные отступы

**Проблема:**
```css
/* ❌ НЕПРАВИЛЬНО */
padding: 12px;
margin: 10px;
```

**Решение:**
```css
/* ✅ ПРАВИЛЬНО */
padding: var(--space-2);  /* 16px */
margin: var(--space-4);   /* 32px */
```

---

### ❌ Ошибка 2: Неправильная структура заголовков

**Проблема:**
```markdown
# Заголовок статьи
# Ещё один H1  ← ❌ НЕПРАВИЛЬНО (два H1)
```

**Решение:**
```markdown
# Заголовок статьи (один H1, генерируется автоматически)

## Основной раздел
### Подраздел
```

---

### ❌ Ошибка 3: Неправильный формат даты

**Проблема:**
```yaml
pubDate: 2026-01-17  ← ❌ БЕЗ кавычек
pubDate: '17.01.2026'  ← ❌ Неправильный формат
```

**Решение:**
```yaml
pubDate: '2026-01-17'  ← ✅ ПРАВИЛЬНО (YYYY-MM-DD, с кавычками)
```

---

### ❌ Ошибка 4: Неправильные пути к изображениям

**Проблема:**
```markdown
![Image](/assets/image.png)  ← ❌ Абсолютный путь
![Image](../assets/image.png)  ← ❌ Неправильный относительный путь
```

**Решение:**
```markdown
![Image](./assets/image.png)  ← ✅ ПРАВИЛЬНО (относительный путь)
```

---

### ❌ Ошибка 5: Слишком длинное описание

**Проблема:**
```yaml
description: 'Очень длинное описание, которое превышает 160 символов и может негативно повлиять на SEO...'  ← ❌ Слишком длинно
```

**Решение:**
```yaml
description: 'Краткое SEO описание 150-160 символов'  ← ✅ ПРАВИЛЬНО
```

---

### ❌ Ошибка 6: Неправильная категория

**Проблема:**
```yaml
category: 'технический-анализ'  ← ❌ Неправильный формат
category: 'tech'  ← ❌ Несуществующая категория
```

**Решение:**
```yaml
category: 'technical-analysis'  ← ✅ ПРАВИЛЬНО
```

---

## ✅ ЧЕКЛИСТ ПЕРЕД ПУБЛИКАЦИЕЙ

Перед публикацией статьи проверьте:

- [ ] Frontmatter заполнен корректно (title, description, pubDate, category)
- [ ] Description 150-160 символов
- [ ] Дата в формате YYYY-MM-DD с кавычками
- [ ] Категория одна из 4 доступных
- [ ] Изображения в папке `assets/` с относительными путями
- [ ] Структура заголовков правильная (один H1, затем H2, H3)
- [ ] Абзацы короткие (2-3 предложения)
- [ ] Списки используются для перечисления
- [ ] Ключевые термины выделены **жирным**
- [ ] Нет хардкод значений spacing (используются CSS переменные)
- [ ] Все ссылки рабочие
- [ ] Код правильно отформатирован

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- [`CONTENT_MIGRATION_GUIDE.md`](./CONTENT_MIGRATION_GUIDE.md) — руководство по миграции контента
- [`CONTENT_MIGRATION_INSTRUCTION.md`](./CONTENT_MIGRATION_INSTRUCTION.md) — инструкция по миграции
- [`../../src/styles/tokens.css`](../../src/styles/tokens.css) — CSS переменные
- [`../../src/styles/nes8bit.css`](../../src/styles/nes8bit.css) — 8-bit стили

---

## 🎯 БЫСТРАЯ СПРАВКА

**Spacing:**
- Минимальный: `var(--space-1)` (8px)
- Стандартный: `var(--space-2)` (16px)
- Между элементами: `var(--space-3)` (24px)
- Между компонентами: `var(--space-4)` (32px)
- Между секциями: `var(--space-6)` (48px)

**Категории:**
- `technical-analysis` — ТЕХАНАЛИЗ
- `crypto` — КРИПТО
- `algo-trading` — АЛГО
- `fundamental-analysis` — ФУНДАМЕНТАЛ

**Формат даты:**
- `pubDate: '2026-01-17'` (YYYY-MM-DD, с кавычками)

**Размер баннера:**
- 1200x630px PNG

---

**Следуя этим правилам, вы гарантируете, что контент будет оформлен правильно и ничего не сломается!** ✅
