# 🎨 Механика OG изображений

## 📍 Где лежат изображения

### 1. **Автоматически сгенерированные OG изображения**
**Путь:** `dist/og/` (после сборки)

**Формат именования:**
- Статьи: `{lang}-{slug}.png` (например: `ru-bitcoin-basics.png`)
- Главная: `homepage.png`

**Примеры:**
```
dist/og/
  ├── ru-bitcoin-basics.png
  ├── ru-what-is-digital-currency-in-simple-terms.png
  ├── en-bitcoin-basics.png
  ├── homepage.png
  └── ...
```

### 2. **Баннеры статей (старый подход)**
**Путь:** `src/content/blog-{lang}/{slug}/assets/banner.jpg`

**Пример:**
```
src/content/blog-ru/what-is-digital-currency-in-simple-terms/
  └── assets/
      └── banner.jpg  ← Баннер для отображения внутри статьи
```

---

## 🔄 Как это работает

### **Автоматическая генерация OG изображений**

1. **При сборке проекта** (`npm run build`):
   - Astro собирает сайт → создаётся `dist/`
   - Автоматически запускается скрипт `scripts/generate-og-images.mjs`
   - Скрипт читает все статьи из `src/content/blog-*/`
   - Генерирует OG изображения в `dist/og/`

2. **Использование в статьях:**
   - `BaseHead.astro` автоматически определяет путь к OG изображению
   - Формат: `/marketlab-academy/og/{lang}-{slug}.png`
   - Если в статье явно указано изображение (prop `image`), используется оно
   - Если нет — используется автоматически сгенерированное

### **Приоритет изображений:**

```
1. Явно указанное изображение (image prop в BaseHead)
   ↓ (если нет)
2. Автоматически сгенерированное OG изображение
   ↓ (если нет)
3. Fallback изображение (blog-placeholder-1.jpg)
```

---

## 🎯 Разница между баннером и OG изображением

| Тип | Назначение | Где используется | Где лежит |
|-----|-----------|------------------|-----------|
| **OG изображение** | Превью в соцсетях/Telegram | Meta tags (og:image, twitter:image) | `dist/og/` (автогенерация) |
| **Баннер статьи** | Визуал внутри статьи | Отображается в контенте статьи | `src/content/.../assets/banner.jpg` |

---

## 📖 Как посмотреть примеры

### **Вариант 1: Через файловый менеджер**
```bash
# Windows
explorer dist\og

# Или просто откройте папку:
D:\Projects\HampfreeBlog\dist\og
```

### **Вариант 2: Через браузер (после деплоя)**
```
https://hampfree-hub.github.io/marketlab-academy/og/ru-bitcoin-basics.png
https://hampfree-hub.github.io/marketlab-academy/og/homepage.png
```

### **Вариант 3: Локально (после сборки)**
```bash
npm run build
npm run preview
# Откройте http://localhost:4321/og/ru-bitcoin-basics.png
```

---

## ❓ Нужна ли папка `assets` в каждой статье?

### **Для OG изображений — НЕТ** ✅
- OG изображения генерируются автоматически
- Не нужно создавать их вручную
- Не нужно добавлять в `assets/`

### **Для баннеров статей — ДА** (опционально) 📸
- Если хотите красивый баннер внутри статьи
- Создайте `assets/banner.jpg` в папке статьи
- Используйте в компоненте статьи через `post.data.banner`

---

## 🔧 Как работает автоматическое определение

В `BaseHead.astro` (строки 47-74):

```typescript
// Автоматически определяем путь к сгенерированному OG изображению
// Формат: /marketlab-academy/og/{lang}-{slug}.png для статей

if (pathname.includes('/library/')) {
  // Извлекаем lang и slug из пути
  const lang = pathParts[langIndex + 1];  // ru, en, es
  const slug = pathParts.slice(langIndex + 2).join('-');  // bitcoin-basics
  ogImageUrl = new URL(`/marketlab-academy/og/${lang}-${slug}.png`, Astro.site);
}
```

**Пример:**
- Статья: `/ru/library/bitcoin-basics/`
- OG изображение: `/marketlab-academy/og/ru-bitcoin-basics.png`

---

## ✅ Итог

1. **OG изображения** → Автоматическая генерация → `dist/og/` → Используются для превью
2. **Баннеры статей** → Ручное добавление → `src/content/.../assets/` → Используются в контенте
3. **Папка `assets`** → Нужна только для баннеров, не для OG изображений

---

## 🚀 Что делать дальше?

1. **Посмотреть примеры:** Откройте `dist/og/` в файловом менеджере
2. **Проверить в браузере:** После деплоя откройте любую статью и проверьте meta tags
3. **Добавить баннеры (опционально):** Если нужны визуалы внутри статей, создайте `assets/banner.jpg`
