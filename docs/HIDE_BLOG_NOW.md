# 🔒 Как скрыть блог СЕЙЧАС

**КРИТИЧНО:** Блог должен быть скрыт от других пользователей!

---

## ✅ Решение: Maintenance Mode

**Как скрыть:**

1. **Создать файл `.env` в корне проекта:**
   ```env
   MAINTENANCE_MODE=true
   ```

2. **Пересобрать сайт:**
   ```powershell
   npm run build
   ```

3. **Запушить на GitHub:**
   ```powershell
   git add .env
   git commit -m "feat: enable maintenance mode"
   git push github main
   ```

**Результат:**
- ✅ Все пользователи увидят страницу "Блог временно недоступен"
- ✅ Блог скрыт от всех
- ✅ Вы можете работать локально (`npm run dev`)

---

## 🔓 Как вернуть блог:

1. **Изменить `.env`:**
   ```env
   MAINTENANCE_MODE=false
   ```

2. **Пересобрать и запушить:**
   ```powershell
   npm run build
   git add .env
   git commit -m "feat: disable maintenance mode"
   git push github main
   ```

---

## ⚠️ ВАЖНО:

**`.env` файл НЕ должен быть в Git!**

**Проверьте `.gitignore`:**
```
.env
```

**Если нет — добавьте!**

---

**Скрыть блог сейчас?** 🔒

