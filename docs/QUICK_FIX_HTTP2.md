# ⚡ Быстрое решение: Отключить HTTP/2 в Cursor

## Способ 1: Через настройки (САМЫЙ ПРОСТОЙ)

### Шаг 1: Открыть настройки
- Нажмите **`Ctrl + ,`** (Ctrl и запятая одновременно)
- Или: File → Preferences → Settings

### Шаг 2: Найти настройку HTTP/2
- В поиске настроек введите: **`http2`**
- Или введите: **`disable http`**

### Шаг 3: Отключить HTTP/2
- Найдите опцию **"Disable HTTP/2"** или **"Network: Disable HTTP/2"**
- Поставьте галочку ✅ (включите опцию)

### Шаг 4: Перезапустить Cursor
- Закройте Cursor полностью
- Запустите заново

### Шаг 5: Проверить
- Settings → Network → "Run Diagnostics"
- Всё должно быть зелёным ✅
- Попробуйте простой запрос в Chat

---

## Способ 2: Через JSON настройки (если не нашли через GUI)

### Шаг 1: Открыть settings.json
- Нажмите **`Ctrl + Shift + P`**
- Введите: **`Preferences: Open Settings (JSON)`**
- Нажмите Enter

### Шаг 2: Добавить настройку
Добавьте эту строку в файл:

```json
{
  "network.disableHttp2": true
}
```

### Шаг 3: Сохранить и перезапустить
- Сохраните файл (**`Ctrl + S`**)
- Перезапустите Cursor

---

## Способ 3: Через меню (альтернативный путь)

1. Нажмите на иконку **шестерёнки** (⚙️) в левом нижнем углу
2. Выберите **Settings**
3. В левом меню найдите **Network**
4. Найдите опцию **"Disable HTTP/2"**
5. Включите её ✅
6. Перезапустите Cursor

---

## ✅ Проверка успешности

После перезапуска:

1. **Откройте Network Diagnostics:**
   - Settings → Network → "Run Diagnostics"
   - Все проверки должны быть зелёными ✅

2. **Попробуйте простой запрос:**
   - Откройте Chat (Ctrl + L)
   - Напишите: "Привет, как дела?"
   - Если ответ пришёл без ошибок - всё работает! ✅

3. **Проверьте, что ошибки исчезли:**
   - Выполните команду в терминале
   - Если нет сообщения "Connection failed" - проблема решена!

---

## 🔍 Если настройка не найдена

Если вы не можете найти настройку "Disable HTTP/2":

1. **Проверьте версию Cursor:**
   - Help → About Cursor
   - Если версия очень старая (< 0.40), обновите

2. **Попробуйте через командную строку:**
   - Закройте Cursor
   - Запустите с флагом:
   ```powershell
   & "C:\Users\$env:USERNAME\AppData\Local\Programs\cursor\Cursor.exe" --disable-http2
   ```

3. **Проверьте через Developer Tools:**
   - Help → Toggle Developer Tools
   - Console → введите: `process.env`
   - Найдите переменные, связанные с HTTP/2

---

## 📝 Что делать дальше

Если отключение HTTP/2 **помогло:**
- ✅ Проблема решена! Можете продолжать работу
- Сохраните эту настройку

Если отключение HTTP/2 **не помогло:**
- Переходите к Решению 2 (PowerShell) из основного документа
- Или используйте чек-лист из `CURSOR_ERRORS_SOLUTION.md`

---

## ⚠️ Важно

После отключения HTTP/2 Cursor будет использовать HTTP/1.1, что может быть немного медленнее, но **стабильнее** и **надёжнее** на большинстве сетей.

---

**Время выполнения:** 2-3 минуты  
**Эффективность:** Решает проблему в 80% случаев ⭐⭐⭐⭐⭐

