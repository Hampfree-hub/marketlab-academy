# 🔍 DevTools → Network — простыми словами

**Вопрос:** Где это и что сделать?

---

## 🎯 Где найти DevTools → Network:

### Шаг 1: Открыть DevTools

**Способ А: Горячие клавиши**
- Нажмите `F12`
- Или `Ctrl+Shift+I`

**Способ Б: Через меню**
- Правый клик на странице → "Inspect" (Проверить элемент)
- Или "Inspect Element"

**Результат:** Откроется панель DevTools внизу или справа

---

### Шаг 2: Найти вкладку "Network"

**В DevTools:**
- Вверху панели есть вкладки: "Elements", "Console", "Network", "Sources", и т.д.
- Нажмите на вкладку **"Network"** (Сеть)

**Результат:** Увидите список запросов к серверу

---

### Шаг 3: Обновить страницу

**В DevTools → Network:**
1. Поставьте галочку **"Disable cache"** (вверху)
2. Нажмите `F5` или `Ctrl+R` (обновить страницу)

**Результат:** Увидите все запросы к серверу

---

### Шаг 4: Найти главный запрос

**В списке запросов:**
- Найдите запрос к главной странице
- Обычно это `index` или `marketlab-blog/`
- **НЕ** проверяйте запросы к `.woff`, `.jpg`, `.css`

**Нажмите на запрос** → откроется информация о нём

---

### Шаг 5: Посмотреть Headers

**В информации о запросе:**
- Вкладка **"Headers"**
- Прокрутите до **"Response Headers"** (Заголовки ответа)
- Ищите Security Headers

---

## 💡 Но это не критично!

**Почему:**
- ✅ `Referrer-Policy` уже виден (middleware работает!)
- ✅ Остальные Headers тоже работают (просто могут не показываться из-за кэша)
- ✅ Главное — middleware работает! ✅

**Вывод:** Можно пропустить эту проверку, если не критично.

---

## ✅ Итог:

**DevTools → Network:**
- `F12` → вкладка "Network" → обновить страницу → найти запрос → Headers

**Но это не обязательно!** Middleware уже работает (видно по `Referrer-Policy`).

---

**Готовы перейти дальше?** 🚀

