# 🎨 Стандарт подчёркивания ссылок (8-bit стиль)

**Дата:** 2026-01-08  
**Статус:** Утверждено

---

## ⚠️ ПРОБЛЕМА

### Текущая реализация:
```css
a:hover {
  color: var(--nes-accent-yellow, #FCFC00);
  text-decoration: underline;
}
```

**Проблемы:**
1. ❌ `text-decoration: underline` — НЕ контролируется отступ от текста
2. ❌ Толщина линии зависит от браузера (непредсказуемо)
3. ❌ Разная длина линий (зависит от длины текста)
4. ❌ Выглядит **дёшево и непрофессионально**
5. ❌ Не соответствует 8-bit стилю (линия слишком тонкая)

---

## ✅ РЕШЕНИЕ: border-bottom

### Новая реализация:
```css
a {
  color: var(--color-accent);
  text-decoration: none;
  border-bottom: 2px solid transparent; /* Резервируем место */
  padding-bottom: 2px; /* Отступ от текста */
  transition: border-color 0.15s steps(3), color 0.15s steps(3);
}

a:hover {
  color: var(--nes-accent-yellow, #FCFC00);
  border-bottom-color: var(--nes-accent-yellow, #FCFC00);
  /* NO text-decoration */
}
```

**Преимущества:**
1. ✅ **Контролируем отступ** — `padding-bottom: 2px`
2. ✅ **Контролируем толщину** — `border-bottom: 2px solid`
3. ✅ **Унифицированная длина** — точно под текстом
4. ✅ **8-bit стиль** — чёткая линия, не размытая
5. ✅ **steps() transition** — пиксельная анимация
6. ✅ **Профессионально** — предсказуемо на всех устройствах

---

## 📐 СТАНДАРТ

### Обычные ссылки (в тексте):
```css
a {
  color: var(--color-accent);            /* Зелёный */
  text-decoration: none;
  border-bottom: 2px solid transparent;  /* Резерв места */
  padding-bottom: 2px;                   /* Отступ 2px */
  transition: border-color 0.15s steps(3), 
              color 0.15s steps(3);
}

a:hover {
  color: var(--nes-accent-yellow);       /* Жёлтый */
  border-bottom-color: var(--nes-accent-yellow); /* Линия жёлтая */
}
```

**Параметры:**
- Толщина линии: `2px` (8-bit стиль)
- Отступ от текста: `2px` (читаемо)
- Цвет при hover: жёлтый (`#FCFC00`)
- Transition: `steps(3)` (пиксельная анимация)

---

### Исключения (без подчёркивания):

#### 1. Кнопки-ссылки
```css
.btn,
.tag-btn,
.read-more-link {
  border-bottom: none !important; /* Без подчёркивания */
}

.btn:hover,
.tag-btn:hover,
.read-more-link:hover {
  text-decoration: none;
  border-bottom: none;
}
```

**Почему:** У кнопок уже есть свой стиль (border, bulge)

---

#### 2. Навигация
```css
.nav-link {
  border-bottom: 4px solid transparent; /* Bulge вместо underline */
}

.nav-link:hover {
  border-bottom-color: var(--nes-accent-green-dark);
  /* NO underline */
}
```

**Почему:** Навигация использует bulge-эффект

---

#### 3. Карточки постов
```css
.post-card a {
  border-bottom: none; /* Без подчёркивания */
}

.post-card a:hover {
  text-decoration: none;
  border-bottom: none;
}
```

**Почему:** У карточек свой hover-эффект (bulge)

---

## 🎯 ПРИМЕНЕНИЕ

### Файлы для обновления:

1. **`src/styles/global.css`** ✅ ГЛАВНЫЙ
   - Базовые стили `a` и `a:hover`
   - Исключения для `.btn`, `.read-more-link`

2. **`src/styles/nes8bit.css`** ✅ ДУБЛИРУЕТ
   - Дублирует стили из `global.css`
   - Обновить для консистентности

3. **Проверить компоненты:**
   - `Header.astro` — навигация (уже bulge) ✅
   - `Footer.astro` — ссылки в футере (обновить)
   - Любые `.astro` с ссылками в тексте

---

## 📋 CHECKLIST

### Обязательно проверить:

- [ ] Главная страница — ссылки в тексте
- [ ] Страница блога — ссылки в постах
- [ ] Страница "О нас" — ссылки в тексте
- [ ] Футер — ссылки (социальные, навигация)
- [ ] Карточки постов — убедиться, что НЕТ underline
- [ ] Кнопки — убедиться, что НЕТ underline
- [ ] Навигация — bulge работает, НЕТ underline

---

## 🔍 ТЕСТИРОВАНИЕ

### Визуальная проверка:

1. **Наведение на обычную ссылку:**
   - Текст → жёлтый ✅
   - Линия → жёлтая, 2px, отступ 2px ✅
   - Анимация → steps(3) (не smooth) ✅

2. **Наведение на кнопку:**
   - НЕТ underline ✅
   - Bulge работает ✅

3. **Наведение на навигацию:**
   - НЕТ underline ✅
   - Bulge работает ✅

4. **Разные устройства:**
   - Desktop — одинаково ✅
   - Tablet — одинаково ✅
   - Mobile — одинаково ✅

---

## 💻 КОД ДЛЯ РЕАЛИЗАЦИИ

### global.css (обновление):

```css
/* ═══════════════════════════════════════════════════════════
   LINKS — 8-bit Style with Controlled Underline
   ═══════════════════════════════════════════════════════════ */

a {
  color: var(--color-accent);
  text-decoration: none;
  border-bottom: 2px solid transparent; /* Reserve space for underline */
  padding-bottom: 2px; /* Distance from text */
  transition: border-color 0.15s steps(3), 
              color 0.15s steps(3);
}

a:hover {
  color: var(--nes-accent-yellow, #FCFC00);
  border-bottom-color: var(--nes-accent-yellow, #FCFC00);
}

/* Exceptions: Buttons and special links */
.btn,
.tag-btn,
.read-more-link,
.nav-link,
.lang-btn,
.theme-btn,
.post-card a {
  border-bottom: none !important;
  padding-bottom: 0;
}

.btn:hover,
.tag-btn:hover,
.read-more-link:hover,
.post-card:hover .read-more-link {
  text-decoration: none;
  border-bottom: none;
}

/* Footer links — keep underline style */
.footer-links a {
  border-bottom: 2px solid transparent;
  padding-bottom: 2px;
}

.footer-links a:hover {
  border-bottom-color: var(--nes-accent-yellow);
}
```

---

## 🎨 ВИЗУАЛЬНОЕ СРАВНЕНИЕ

### ДО (text-decoration):
```
┌─────────────────────────┐
│ This is a link          │ ← Текст
│ ▔▔▔▔▔▔▔▔▔▔▔▔▔▔          │ ← Линия (непредсказуемая)
└─────────────────────────┘
```

**Проблемы:**
- ❌ Отступ от текста — случайный
- ❌ Толщина — зависит от браузера
- ❌ Может быть размытой на некоторых экранах

---

### ПОСЛЕ (border-bottom):
```
┌─────────────────────────┐
│ This is a link          │ ← Текст
│                         │ ← Отступ 2px (контролируем)
│ ▀▀▀▀▀▀▀▀▀▀▀▀▀▀          │ ← Линия 2px (чёткая)
└─────────────────────────┘
```

**Преимущества:**
- ✅ Отступ 2px — всегда одинаковый
- ✅ Толщина 2px — чёткая
- ✅ Профессионально на всех устройствах

---

## 📊 ИТОГО

### Параметры стандарта:

| Параметр | Значение | Почему |
|----------|----------|--------|
| Метод | `border-bottom` | Контроль отступа и толщины |
| Толщина линии | `2px` | 8-bit стиль (чёткая линия) |
| Отступ от текста | `2px` | Читаемо, не слипается |
| Цвет hover | `#FCFC00` (жёлтый) | NES palette |
| Transition | `steps(3)` | Пиксельная анимация |
| Исключения | Кнопки, навигация, карточки | Свои hover-эффекты |

---

**Обновлено:** 2026-01-08  
**Статус:** Ready for implementation
