# 🔍 ДИАГНОСТИКА OG ИЗОБРАЖЕНИЙ (404 ОШИБКА)

**Дата:** 2026-01-20

---

## ❌ ПРОБЛЕМА

**404 ошибка** при попытке открыть OG изображения:
- `https://hampfree-hub.github.io/marketlab-academy/og/homepage.png` → 404

---

## 🔍 ЧТО ПРОВЕРИТЬ

### 1. Логи GitHub Actions

**После деплоя:**
1. Откройте: https://github.com/Hampfree-hub/marketlab-academy/actions
2. Выберите последний workflow run
3. Откройте шаг "Build"
4. Найдите в логах:
   - `[OG Images] Script started`
   - `[OG Images] Starting OG image generation...`
   - `[OG Images] Found X posts`
   - `[OG Images] Generated X images successfully`

**Если этих логов нет:**
- Скрипт не запускается или падает до начала выполнения
- Проверить, что `npm run build` включает скрипт

**Если есть ошибки:**
- Скопировать полный текст ошибки
- Проверить, что все зависимости установлены

---

### 2. Проверить структуру dist/ после сборки

**В GitHub Actions:**
- После шага "Build" должен быть артефакт
- Проверить, есть ли папка `og/` в артефакте

**Локально:**
```bash
npm run build
ls -la dist/og/  # или dir dist\og в Windows
```

---

### 3. Возможные причины 404

**Причина 1: Скрипт не выполняется**
- Проверить `package.json`: `"build": "astro build && node scripts/generate-og-images.mjs"`
- Проверить логи GitHub Actions

**Причина 2: Скрипт падает с ошибкой**
- Проверить логи на наличие ошибок
- Возможные проблемы:
  - `fetch` не доступен (Node.js < 18)
  - Ошибка импорта `satori` или `@resvg/resvg-js`
  - Ошибка при генерации SVG/PNG

**Причина 3: Изображения генерируются, но не деплоятся**
- Проверить, что папка `og/` попадает в артефакт
- Проверить, что GitHub Pages деплоит всё из `dist/`

---

## 🛠️ БЫСТРЫЕ ИСПРАВЛЕНИЯ

### Если скрипт не запускается:

**Проверить package.json:**
```json
{
  "scripts": {
    "build": "astro build && node scripts/generate-og-images.mjs"
  }
}
```

### Если ошибка с fetch:

**Node.js < 18 не поддерживает fetch.**
- В GitHub Actions используется Node.js 20 (должно работать)
- Локально: обновить Node.js до версии 18+

### Если ошибка с зависимостями:

**Проверить, что установлены:**
```bash
npm install satori @resvg/resvg-js react
```

---

## 📋 ПЛАН ДЕЙСТВИЙ

1. **Дождаться деплоя** (2-5 минут)
2. **Проверить логи GitHub Actions** на наличие `[OG Images]` сообщений
3. **Если логов нет** → скрипт не запускается → проверить `package.json`
4. **Если есть ошибки** → скопировать и исправить
5. **Если всё ок, но 404** → проверить, что папка `og/` в артефакте

---

**После проверки логов GitHub Actions станет ясно, в чём проблема!** 🔍
