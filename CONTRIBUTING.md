# 🤝 Contributing Guide | MarketLab Academy

**Для разработчиков блога (solo & team)**

---

## 🎯 ПРОСТОЙ АЛГОРИТМ РАБОТЫ

### **Для тебя (Owner):**

**Ты говоришь:** "Нужно сделать X"  
**Я делаю:** Issue → Branch → Code → PR → Merge

**Ты утверждаешь:** "Ок" / "Поправь Y"  
**Я фиксирую:** Merge → Cleanup → Next task

---

### **Для AI Assistant (меня):**

```
┌─────────────────────────────────────────┐
│  1. ISSUE                               │
│  Создать в GitHub Projects              │
│  Title: "Short description"             │
│  Labels: feature/bug/docs               │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  2. BRANCH                              │
│  git checkout main                      │
│  git pull origin main                   │
│  git checkout -b feature/N-description  │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  3. CODE                                │
│  Делать изменения                       │
│  git commit -m "feat: ..." (#N)         │
│  (несколько коммитов ок)                │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  4. PUSH                                │
│  git push -u origin feature/N-...       │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  5. PULL REQUEST                        │
│  Создать PR на GitHub                   │
│  Base: main ← ВАЖНО!                    │
│  Compare: feature/N-...                 │
│  Заполнить описание                     │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  6. REVIEW                              │
│  Показать пользователю                  │
│  "Готово! Смотри PR #N"                 │
│  Ждать одобрения                        │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  7. MERGE (после "Ок!")                 │
│  git checkout main                      │
│  git pull origin main                   │
│  git merge --squash feature/N-...       │
│  git commit -m "..." (хорошее описание) │
│  git push origin main                   │
└─────────────────────────────────────────┘
           ↓
┌─────────────────────────────────────────┐
│  8. CLEANUP                             │
│  git branch -d feature/N-...            │
│  git push origin --delete feature/N-... │
│  Закрыть issue (если Closes #N)        │
└─────────────────────────────────────────┘
```

---

## 📋 NAMING CONVENTIONS

### **Branches:**
```
feature/123-short-description   — новая фича
bugfix/124-fix-something        — баг-фикс
refactor/125-cleanup            — рефакторинг
docs/126-update-guide           — документация
experiment/127-test-idea        — эксперимент
hotfix/128-critical-fix         — срочное исправление
```

### **Commits:**
```
feat: add new feature (#123)
fix: resolve bug (#124)
refactor: cleanup code (#125)
docs: update guide (#126)
test: add tests (#127)
chore: update dependencies
```

### **PR Titles:**
```
feat: Short description (что сделано)
fix: Short description (что исправлено)
refactor: Short description (что улучшено)
```

---

## ✅ PR DESCRIPTION TEMPLATE

```markdown
## 🎯 Цель
Что делаем и зачем.

## ✅ Что сделано
- [x] Task 1
- [x] Task 2
- [x] Task 3

## 🔗 Связанные Issues
Closes #123
Related to #124

## 🧪 Как протестировано
Локально: описание
Staging: ссылка (если есть)

## 📋 Checklist
- [x] Код работает
- [x] Нет хардкода
- [x] Документация обновлена
- [x] Security: нет секретов
```

---

## 🚨 ПРАВИЛА (СТРОГО!)

### **1. НИКОГДА не коммитить в `main` напрямую**
```bash
git checkout main
git commit -m "fix: something"  ❌ НЕЛЬЗЯ!
```

**Исключения:**
- ✅ Опечатки в README (1-2 строки)
- ✅ Обновление версии
- ✅ Критический hotfix (с согласия owner)

---

### **2. ВСЕГДА создавать ветку от `main`**
```bash
# ПРАВИЛЬНО:
git checkout main
git pull origin main
git checkout -b feature/new-task ✅

# НЕПРАВИЛЬНО:
git checkout feature/old-branch
git checkout -b feature/new-task ❌
```

---

### **3. PR должен быть МАЛЕНЬКИМ**

| Размер | Файлов | Строк | Статус |
|--------|--------|-------|--------|
| Tiny | 1-3 | < 50 | 🟢 Отлично |
| Small | 4-10 | 50-200 | 🟢 Хорошо |
| Medium | 11-20 | 200-500 | 🟡 Ок |
| Large | 21-50 | 500-1000 | 🟠 Много |
| Huge | 50+ | 1000+ | 🔴 Разделить! |

**Правило:** Если > Medium → разделить на несколько PR!

---

### **4. Squash and Merge (всегда!)**

Используем **Squash and merge**, чтобы история `main` оставалась чистой:
- ✅ Один commit на фичу
- ✅ Понятное описание
- ✅ Легко откатить

---

### **5. NO SECRETS в коде!**

```bash
# НЕЛЬЗЯ:
const API_KEY = "sk_live_123..."  ❌

# ПРАВИЛЬНО:
const API_KEY = process.env.API_KEY  ✅
```

**Перед каждым commit:**
```bash
git diff | grep -i "token\|key\|secret\|password"
```

---

## 🤖 ДЛЯ AI ASSISTANT (автоматизация)

### **После merge автоматически:**
```bash
# 1. Checkout main
git checkout main

# 2. Pull changes
git pull origin main

# 3. Delete branch
git branch -d feature/N-description
git push origin --delete feature/N-description

# 4. Update docs (if needed)
# ...

# 5. Report to user
"✅ PR #N merged! Changes in main. Next task?"
```

---

## 📊 СТАТУС ISSUE / PR

### **Issue Labels:**
- `feature` — новая фича
- `bug` — баг
- `refactor` — рефакторинг
- `docs` — документация
- `enhancement` — улучшение
- `urgent` — срочно
- `low-priority` — низкий приоритет
- `needs-testing` — требует тестирования
- `wontfix` — не будем исправлять

### **Issue Status:**
- `Open` — в работе
- `Closed` — выполнено

### **PR Status:**
- `Open` — на ревью
- `Merged` — влито
- `Closed` — отклонено

---

## 🔄 КОГДА ПОЯВИТСЯ КОМАНДА

**Добавится:**
1. **Code Review** (минимум 1 reviewer)
2. **Approve** перед merge
3. **GitHub Actions** (тесты, линтеры)
4. **Branch Protection** (нельзя пушить в main)

**Процесс останется тот же!** Просто добавится шаг "ждать Approve".

---

## 📚 ПОЛЕЗНЫЕ КОМАНДЫ

### **Проверить статус:**
```bash
git status
git branch              # Список веток
git log --oneline       # История коммитов
```

### **Синхронизация:**
```bash
git fetch origin        # Обновить информацию о remote
git pull origin main    # Подтянуть изменения
git push origin branch  # Запушить ветку
```

### **Откат изменений:**
```bash
git restore file.txt    # Откатить изменения в файле
git reset HEAD~1        # Откатить последний commit (локально)
```

### **Cleanup:**
```bash
git branch -d feature/N  # Удалить локальную ветку
git branch -D feature/N  # Удалить принудительно
git push origin --delete feature/N  # Удалить remote ветку
```

---

## 🎯 БЫСТРЫЙ СТАРТ (для новых разработчиков)

```bash
# 1. Клонировать репо
git clone https://github.com/Hampfree-hub/marketlab-academy.git
cd marketlab-academy

# 2. Установить зависимости
npm install

# 3. Запустить dev-сервер
npm run dev

# 4. Открыть в браузере
http://localhost:4321

# 5. Создать ветку для задачи
git checkout -b feature/your-task

# 6. Код → Commit → Push → PR!
```

---

## 📖 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Workflow Guide:** `docs/WORKFLOW_PR_OPTIMIZED.md` (Private repo)
- **8-bit UI Guidelines:** `docs/8BIT_UI_GUIDELINES.md` (Private repo)
- **GitHub Projects:** https://github.com/users/Hampfree-hub/projects/1/views/1

---

**Дата создания:** 2026-01-09  
**Статус:** ✅ Active  
**Версия:** 1.0
