# 🔒 Maintenance Mode — работа локально

**Вопрос:** Как работать локально когда блог скрыт онлайн?

---

## ✅ Решение:

### **Как работает Maintenance Mode:**

**Онлайн (GitHub Pages):**
- ✅ Если `MAINTENANCE_MODE=true` → показывается страница maintenance
- ✅ Все пользователи видят "Блог временно недоступен"

**Локально (`npm run dev`):**
- ✅ Maintenance Mode НЕ работает в dev-режиме
- ✅ Вы видите обычный блог
- ✅ Можете работать и тестировать

**Почему:**
- Middleware проверяет `import.meta.env.MAINTENANCE_MODE`
- В dev-режиме переменные из `.env` могут не загружаться
- Или можно настроить чтобы не работало локально

---

## 🔧 Как настроить:

### **Вариант 1: Не проверять в dev-режиме** ⭐⭐⭐⭐⭐ (РЕКОМЕНДУЮ!)

**В middleware.ts:**
```typescript
// Проверять только в production
if (import.meta.env.PROD && import.meta.env.MAINTENANCE_MODE === 'true') {
  // Redirect to maintenance
}
```

**Результат:**
- ✅ Локально (`npm run dev`) — всегда видите блог
- ✅ Онлайн (production) — работает Maintenance Mode

---

### **Вариант 2: Отдельная переменная**

**В .env:**
```env
MAINTENANCE_MODE=true
MAINTENANCE_MODE_LOCAL=false
```

**В middleware.ts:**
```typescript
const isMaintenance = import.meta.env.PROD 
  ? import.meta.env.MAINTENANCE_MODE === 'true'
  : import.meta.env.MAINTENANCE_MODE_LOCAL === 'true';
```

---

## ✅ Рекомендация:

**Вариант 1** — самый простой и удобный!

**Как работает:**
- ✅ Локально (`npm run dev`) — всегда видите блог
- ✅ Онлайн (production) — работает Maintenance Mode
- ✅ Не нужно ничего менять локально

---

**Обновить middleware для работы локально?** 🔒

