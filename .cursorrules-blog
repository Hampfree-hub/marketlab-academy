# Правила работы с блогом MarketLab Academy

## ⚠️ ВАЖНО: Подтверждение перед действиями

**ПРАВИЛО:** Перед выполнением действий, которые могут изменить код или настройки, ВСЕГДА спрашивать подтверждение у пользователя.

**Примеры действий, требующих подтверждения:**
- Обновление файлов конфигурации
- Изменение путей (base path, routes)
- Обновление зависимостей
- Изменение структуры проекта
- Запуск/остановка серверов
- Коммиты и push

**Формат запроса:**
```
⚠️ ВНИМАНИЕ: Я собираюсь [описание действия]

Это изменит:
- [что изменится 1]
- [что изменится 2]

Продолжить? (да/нет)
```

**Исключения (не требуют подтверждения):**
- Чтение файлов
- Поиск в коде
- Создание новых файлов (документация, планы)
- Проверка статуса

---

## 📋 Другие правила:

- Всегда проверять что dev сервер запущен перед изменениями
- Коммитить важные изменения поэтапно
- Не коммитить токены и секреты
- Проверять сборку после изменений

## 🔍 Правило проверки смежных компонентов (КРИТИЧЕСКИ ВАЖНО!)

**ПРАВИЛО:** При выполнении любой задачи **ОБЯЗАТЕЛЬНО** проверять смежные компоненты и адаптивность на всех экранах!

### Что проверять:

1. **Адаптивность (ОБЯЗАТЕЛЬНО!):**
   - Десктоп (> 1024px)
   - Планшеты (768px - 1024px)
   - Мобильные (< 768px)

2. **Смежные компоненты:**
   - Компоненты, использующие те же CSS-классы
   - Компоненты рядом в DOM
   - Компоненты с похожей функциональностью
   - Глобальные стили, которые могут влиять

3. **Отступы и расстояния:**
   - Отступы от краёв экрана
   - Отступы между блоками
   - Отступы для фиксированных элементов (sticky/fixed)

4. **Интерактивность:**
   - Hover-эффекты на всех экранах
   - Active-состояния на мобильных
   - Focus-состояния для клавиатуры
   - Touch-таргеты ≥ 44×44px (WCAG)

**Пример:** При изменении навигации проверяй:
- ✅ Все кнопки на странице (CTA, cookie banner)
- ✅ Другие навигационные элементы
- ✅ Sticky-элементы
- ✅ Отступы контента

**Подробности:** См. `RESPONSIVE_DESIGN_RULES.md`
