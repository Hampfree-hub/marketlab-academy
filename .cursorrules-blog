# 🕵️ ПРОТОКОЛ "ШЕРЛОК" (MarketLab Academy Edition) v2.3 — ZERO IMAGES

Этот файл содержит КРИТИЧЕСКИЕ инструкции для AI-ассистента по работе с блогом.
НЕ КОММИТИТЬ этот файл в публичный репозиторий!

## 🔴 СТОП-ЛИСТ (Самые частые ошибки)

1. **ЗАГОЛОВКИ (H1-H3):**
   - ❌ `# Заголовок` в тексте статьи ЗАПРЕЩЁН! (H1 уже есть в Layout).
   - ❌ `## **Жирный**` в заголовках ЗАПРЕЩЁН! Только чистый текст.
   - ❌ `####` (H4) ЗАПРЕЩЕНЫ! Используй H3.
   - ❌ Точки, !, ? в конце заголовков ЗАПРЕЩЕНЫ.

2. **КОНТЕНТ И ОФОРМЛЕНИЕ:**
   - ❌ `## Связанные материалы` в конце MD-файлов ЗАПРЕЩЕНЫ! (Используется автоматический компонент `RelatedPosts`).
   - ❌ Эмодзи в тексте статьи — ЗАПРЕЩЕНЫ (только SVG-иконки или спецсимволы в системных блоках).
   - ❌ Пустые `alt` у картинок ЗАПРЕЩЕНЫ.
   - ❌ **Внутренние кавычки в YAML:** `description: "текст "в кавычках""` — ЗАПРЕЩЕНО! Используй одинарные или убери.

3. **ТЕХНИЧЕСКИЕ ПРАВИЛА:**
   - ❌ Не менять структуру URL без согласования.
   - ❌ Не использовать Cyrillic в названиях ключей локализации или латинских словах (TEMA vs ТЕМА).
   - ❌ Не коммитить `.cursorrules` или `.env`.
   - ❌ **Формат Дат:** RU (DD.MM.YYYY), EN (MM/DD/YYYY), ES (DD.MM.YYYY). ОШИБКА ФОРМАТА ЛОМАЕТ СБОРКУ!

---

## 🏷️ ПРИНЦИП "ZERO IMAGES" (НОВИНКА!)

1. **Никаких внешних PNG/JPG для баннеров.** Все превью статей и заглавные баннеры рисуются компонентом `<Banner />` на лету с использованием CSS-паттернов.
2. **Иконки только кодом.** Для категорий и элементов интерфейса используем `<PixelIcon />` (техника CSS box-shadow).
3. **NES-UI компоненты.** Все блоки (сайдбары, формы, связанные посты) оборачиваем в `.nes-container` с двойной рамкой.

---

## 📋 ПРАВИЛА ТИПОГРАФИКИ (8-bit NES Style)

- **Grid:** Все отступы (margin, padding) должны быть КРАТНЫ 8px (8, 16, 24, 32, 48, 64, 80).
- **Typography:** 
  - Текст: 'JetBrains Mono', 1.7-1.8 line-height.
  - Заголовки: 'Press Start 2P', uppercase (для H2).
- **Borders:** Только прямые углы (0px radius). Пиксельные границы 2px или 4px.
- **Transitions:** Отключены (none) или steps(3/4). Никакой плавной анимации.

---

## 🛠 АЛГОРИТМ "ШЕРЛОК" (Deep Debugging)

При возникновении проблем:
1. **Сбор улик:** Проверь консоль браузера, логи терминала и сетевые запросы.
2. **Анализ ДНК:** Проверь, не перекрываются ли стили новыми переменными или `nes8bit.css`.
3. **Локализация:** Убедись, что ключи в `ui.ts` существуют во всех языках (RU, EN, ES).
4. **Связи:** Если сломался один компонент, проверь `global.css` и `base.css` — возможно, там глобальный конфликт.
5. **Проверка ссылок:** Перед сохранением убедись, что ссылки на другие статьи ведут на существуные ID.

---

## 🔍 ЧЕК-ЛИСТ ПЕРЕД КОММИТОМ

1. [ ] Удалены ли дублирующиеся H1 из статей?
2. [ ] Удалены ли ручные блоки "Связанные материалы"?
3. [ ] Проверена ли мобильная версия (шапка, меню, отступы)?
4. [ ] Используется ли `<Banner />` вместо `previewImage` в карточках?
5. [ ] Обернуты ли важные блоки в `.nes-container`?
6. [ ] Использованы ли только Latin символы в испанском переводе (TEMA)?
7. [ ] Проверены ли YAML-кавычки в `description`?
8. [ ] Нет ли наложения (overlap) элементов в шапке на разных экранах?
9. [ ] Отступы между кнопками и блоками кратны 8px?

**ДЕВИЗ ШЕРЛОКА:** "Отсекайте всё лишнее, и то, что останется, будет истиной, какой бы невероятной она ни казалась."
