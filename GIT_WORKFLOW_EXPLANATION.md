# 🔀 Рабочий процесс с двумя репозиториями

## 📋 Текущая настройка

У нас **два remote репозитория**:

1. **`origin`** → GitLab (основной, для разработки)
   - `https://gitlab.com/hampfree-team-group/hampfree-blog.git`

2. **`github`** → GitHub (для GitHub Pages хостинга)
   - `https://github.com/Hampfree-hub/marketlab-blog.git`

---

## ❓ Нужно ли всегда пушить в оба?

### ✅ Да, но можно упростить!

**Сейчас:**
```powershell
git push origin main      # GitLab
git push github main      # GitHub
```

**Можно упростить:**
```powershell
git push origin main github main    # Оба сразу
```

Или настроить автоматическую синхронизацию (см. ниже).

---

## 🎯 Зачем два репозитория?

1. **GitLab** — основной репозиторий для разработки
   - История коммитов
   - Backup кода
   - Работа в команде (если будет)

2. **GitHub** — для хостинга через GitHub Pages
   - Автоматический деплой через GitHub Actions
   - Публичный доступ к блогу

---

## 🔧 Варианты упрощения

### Вариант 1: Push в оба сразу (проще)

Создайте алиас в PowerShell:

```powershell
# Добавить в профиль PowerShell (один раз)
function git-push-all {
    git push origin main
    git push github main
}

# Использование:
git-push-all
```

### Вариант 2: GitLab CI для автоматической синхронизации

Настроить GitLab CI чтобы автоматически пушить в GitHub при каждом коммите.

**Файл:** `.gitlab-ci.yml`
```yaml
sync-to-github:
  stage: deploy
  script:
    - git push https://$GITHUB_TOKEN@github.com/Hampfree-hub/marketlab-blog.git main
  only:
    - main
```

**Плюсы:**
- Автоматическая синхронизация
- Не нужно пушить вручную в GitHub

**Минусы:**
- Нужно настроить CI
- Нужен GitHub токен в GitLab CI variables

### Вариант 3: Только GitHub (если GitLab не нужен)

Если GitLab не используется активно, можно работать только с GitHub:

```powershell
# Удалить GitLab remote
git remote remove origin

# GitHub сделать основным
git remote rename github origin

# Теперь просто:
git push origin main
```

---

## 💡 Рекомендация

**Для начала:** Используйте Вариант 1 (push в оба сразу) — проще всего.

**Потом:** Если GitLab не используется активно, можно перейти на Вариант 3 (только GitHub).

---

## 📝 Итого

- ✅ **Сейчас:** Нужно пушить в оба репозитория
- ✅ **Можно упростить:** Push в оба сразу одной командой
- ✅ **Можно автоматизировать:** Через GitLab CI
- ✅ **Можно упростить:** Работать только с GitHub

**Что предпочитаете?** 🤔





