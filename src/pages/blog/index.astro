---
import { Image } from 'astro:assets';
import { getCollection } from 'astro:content';
import ArticleGrid from '../../components/ArticleGrid.astro';
import BaseHead from '../../components/BaseHead.astro';
<<<<<<< HEAD
import CookieBanner from '../../components/CookieBanner.astro';
=======
import BannerCarousel from '../../components/BannerCarousel.astro';
>>>>>>> c522c6a257b6d8fa670661b54ff171fba27829dc
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../../consts';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="ru">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<link rel="stylesheet" href="/src/styles/global.css" />
		<link rel="stylesheet" href="/src/styles/nes8bit.css" />
		<style>
<<<<<<< HEAD
			body {
				background: var(--nes-white);
				color: var(--nes-black);
				font-family: 'Arial', sans-serif;
			}

			.nes8bit-container {
				max-width: 1200px;
				margin: 0 auto;
				padding: 20px;
			}

			.blog-header {
				background: var(--nes-sky-gradient);
				border: 4px solid var(--nes-black);
				padding: 30px;
				margin-bottom: 30px;
				box-shadow: 8px 8px 0px rgba(0, 0, 0, 0.3);
				text-align: center;
			}

			.blog-header h1 {
				font-family: 'Arial', sans-serif;
				font-size: 2.5rem;
				font-weight: bold;
				color: var(--nes-white);
				text-shadow: 3px 3px 0px var(--nes-black);
				margin: 0;
			}

			.blog-header p {
				font-family: 'Arial', sans-serif;
				font-size: 1.2rem;
				color: var(--nes-white);
				text-shadow: 2px 2px 0px var(--nes-black);
				margin: 10px 0 0 0;
			}

			@media (max-width: 768px) {
				.nes8bit-container {
					padding: 10px;
				}

				.blog-header {
					padding: 20px;
				}

				.blog-header h1 {
					font-size: 2rem;
=======
			main {
				max-width: 1200px;
				margin: 0 auto;
				padding: 2.5em 1em 0 1em;
			}
			ul {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
				gap: 1.5rem;
				list-style-type: none;
				margin: 0;
				padding: 0;
			}
			ul li {
				transition: transform var(--transition-smooth, 0.4s cubic-bezier(0.4, 0, 0.2, 1));
			}
			ul li * {
				text-decoration: none;
			}
			ul li a {
				display: block;
				transition: transform var(--transition-smooth, 0.4s cubic-bezier(0.4, 0, 0.2, 1));
			}
			ul li img {
				margin-bottom: 0.5rem;
				border-radius: 12px;
				transition: box-shadow var(--transition-smooth, 0.4s cubic-bezier(0.4, 0, 0.2, 1));
			}
			.title {
				margin: 0;
				color: var(--text-primary);
				line-height: 1;
				transition: color var(--transition-smooth, 0.4s cubic-bezier(0.4, 0, 0.2, 1));
			}
			.date {
				margin: 0;
				color: var(--text-secondary);
				transition: color var(--transition-smooth, 0.4s cubic-bezier(0.4, 0, 0.2, 1));
			}
			ul li:hover a {
				transform: translateY(-4px);
			}
			ul li:hover .title,
			ul li:hover .date {
				color: var(--green-primary, var(--accent));
			}
			ul li:hover img {
				box-shadow: var(--box-shadow);
			}
			/* Убираем двойное выдвижение - только карточка двигается */
			ul li a img {
				transform: none;
			}
			@media (max-width: 720px) {
				ul {
					grid-template-columns: 1fr;
					gap: 1.5rem;
>>>>>>> c522c6a257b6d8fa670661b54ff171fba27829dc
				}
			}
		</style>
	</head>
	<body class="nes8bit-theme">
		<CookieBanner />
		<Header />
<<<<<<< HEAD
		<div class="nes8bit-container">
			<div class="blog-header">
				<h1>📚 Блог Market Lab Academy</h1>
				<p>Восстановленные материалы и эксперименты</p>
			</div>
			<ArticleGrid />
		</div>
=======
		<BannerCarousel />
		<main>
			<section>
				<ul>
					{
						posts.map((post) => (
							<li>
								<a href={`/blog/${post.id}/`}>
									{post.data.heroImage && (
										<Image width={720} height={360} src={post.data.heroImage} alt="" />
									)}
									<h4 class="title">{post.data.title}</h4>
									<p class="date">
										<FormattedDate date={post.data.pubDate} />
									</p>
								</a>
							</li>
						))
					}
				</ul>
			</section>
		</main>
>>>>>>> c522c6a257b6d8fa670661b54ff171fba27829dc
		<Footer />
	</body>
</html>
