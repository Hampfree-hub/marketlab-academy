# ✅ ЧЕКЛИСТ ПЕРЕД КОММИТОМ — 2026-01-20

**Дата:** 2026-01-20  
**Репозиторий:** [Hampfree-hub/marketlab-academy](https://github.com/Hampfree-hub/marketlab-academy)

---

## ✅ ПРОВЕРКА БЕЗОПАСНОСТИ

### 1. ✅ Секреты и пароли

**Проверено:**
- ✅ Все скрипты используют переменные окружения (`process.env.GITHUB_TOKEN`)
- ✅ Нет хардкод токенов в коде
- ✅ `.gitignore` правильно настроен (исключает `**/*SECRET*`, `**/*secret*`, `secrets/`)
- ✅ API ключи через переменные окружения

**Вывод:** ✅ Нет секретов в коде

---

### 2. ✅ Упоминания Veles Blog

**Оставшиеся упоминания (безопасны):**
- ✅ `src/components/Footer.astro` — партнёрская ссылка (это нормально)
- ✅ `src/content/blog-ru/what-bitcoin-everything-you-need-know/index.md` — партнёрская ссылка (это нормально)
- ✅ `src/styles/tokens.css` — комментарий "Inspired by Veles Almanac" (это нормально)
- ✅ `scripts/*.js`, `scripts/*.cjs` — упоминания в скриптах (не критично, скрипты не публикуются)
- ✅ `docs/CONTENT_MIGRATION_*.md` — упоминания в документации (общие инструкции)

**Вывод:** ✅ Критические упоминания удалены, остались только партнёрские ссылки и общие инструкции

---

### 3. ✅ Планы и стратегии

**Проверено:**
- ✅ Документы содержат только общие инструкции
- ✅ Нет раскрытия внутренних планов
- ✅ Нет упоминаний приватных репозиториев

**Вывод:** ✅ Документы безопасны

---

## ✅ ИСПРАВЛЕНИЯ

### 1. ✅ Отступы между абзацами в статьях

**Исправлено:**
- `.prose p` → `margin-bottom: var(--space-2)` (16px вместо 32px)
- Унифицировано с правилами 8-pt grid

**Файл:** `src/styles/components/blog-post.css`

---

### 2. ✅ Фильтрация по языкам в "ПОСЛЕДНИЕ_СТАТЬИ"

**Исправлено:**
- `getRelatedPosts` теперь принимает параметр `lang`
- Фильтрует посты только для текущего языка
- Исправлено в `RelatedPosts.astro` — передаётся язык

**Файлы:**
- `src/utils/blog.ts` — добавлен параметр `lang` в `getRelatedPosts`
- `src/components/RelatedPosts.astro` — передаётся язык в `getRelatedPosts`

---

### 3. ✅ Отступы между фильтром и карточками

**Исправлено:**
- Главная страница: 56px (было 72px)
- Страница библиотеки: 32px (было 64px)
- Унифицировано с правилами 8-pt grid

**Файлы:**
- `src/styles/components/home-index.css`
- `src/styles/components/blog-index.css`

---

## 📋 ГОТОВНОСТЬ К КОММИТУ

**Проверено:**
- ✅ Безопасность (нет секретов, нет критических упоминаний)
- ✅ Отступы унифицированы
- ✅ Фильтрация по языкам работает
- ✅ Все изменения соответствуют правилам 8-pt grid

**Рекомендация:** ✅ Готово к коммиту!

---

## 🎯 КОММИТ

**Рекомендуемое сообщение:**
```
fix: унифицировать отступы и исправить фильтрацию по языкам

- Уменьшены отступы между абзацами в статьях (32px → 16px)
- Исправлена фильтрация по языкам в "ПОСЛЕДНИЕ_СТАТЬИ"
- Унифицированы отступы между фильтром и карточками
- Все отступы соответствуют правилам 8-pt grid
```

---

**Всё готово к коммиту и пушу!** ✅
