# 🖼️ АВТОМАТИЧЕСКАЯ ГЕНЕРАЦИЯ OG ИЗОБРАЖЕНИЙ

**Дата:** 2026-01-20

---

## ✅ РЕАЛИЗОВАНО

**Автоматическая генерация OG изображений из HTML/CSS** на основе компонента `Banner.astro`.

### Как это работает:

1. **Build-time генерация:**
   - После сборки (`npm run build`) автоматически запускается скрипт `scripts/generate-og-images.mjs`
   - Используется `satori` (HTML/JSX → SVG) + `@resvg/resvg-js` (SVG → PNG)
   - Изображения сохраняются в `dist/og/` (после деплоя доступны как `/marketlab-academy/og/...`)

2. **Автоматическое определение пути:**
   - `BaseHead.astro` автоматически определяет путь к OG изображению на основе URL страницы
   - Формат: `/marketlab-academy/og/{lang}-{slug}.png` для статей
   - Формат: `/marketlab-academy/og/homepage.png` для главной

3. **Стилизация:**
   - OG изображения используют те же стили, что и `Banner.astro`
   - 8-bit NES тема, категории, цвета — всё автоматически

---

## 📋 ЧТО НУЖНО СДЕЛАТЬ

### 1. Установлены зависимости:
```bash
npm install satori @resvg/resvg-js react
```
✅ **Готово**

### 2. Созданы файлы:
- ✅ `src/utils/og-image.tsx` — React компонент для OG изображения
- ✅ `scripts/generate-og-images.mjs` — Post-build скрипт для генерации OG изображений
- ✅ Обновлён `package.json` — скрипт запускается автоматически после `npm run build`
- ✅ Обновлён `BaseHead.astro` — автоматическое определение пути к OG изображениям

### 3. Тестирование:

**Проверить генерацию:**
```bash
npm run build
```

**Проверить результат:**
- После сборки в `dist/og/` должны появиться PNG файлы
- Проверить, что изображения корректно отображаются

**Проверить в браузере:**
- Открыть любую статью
- Проверить `<meta property="og:image">` в `<head>`
- URL должен указывать на `/marketlab-academy/og/{lang}-{slug}.png`

---

## 🎯 РЕЗУЛЬТАТ

**После сборки:**
- ✅ Все статьи автоматически получают OG изображения
- ✅ Главная страница получает OG изображение
- ✅ Изображения генерируются из HTML/CSS (как и баннеры)
- ✅ Никаких ручных действий не требуется

**Превью в соцсетях:**
- Telegram, Twitter, Facebook будут показывать кастомные OG изображения
- Стиль соответствует дизайну блога (8-bit NES)

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Шрифты:
- Используются Google Fonts (Press Start 2P, JetBrains Mono)
- Загружаются во время сборки через fetch

### Размер изображений:
- 1200x630px (стандарт Open Graph)

### Кэширование:
- Изображения генерируются только при сборке
- Если контент не изменился, можно добавить кэширование (опционально)

---

## 🔧 ВОЗМОЖНЫЕ УЛУЧШЕНИЯ

1. **Кэширование:**
   - Проверять хеш контента перед генерацией
   - Пропускать генерацию, если изображение уже существует и контент не изменился

2. **Оптимизация:**
   - Генерировать WebP вместо PNG (меньше размер)
   - Использовать более быстрые алгоритмы

3. **Fallback:**
   - Если генерация не удалась, использовать fallback изображение

---

**Всё автоматизировано! Просто запустите `npm run build` и OG изображения будут сгенерированы автоматически.** ✅
