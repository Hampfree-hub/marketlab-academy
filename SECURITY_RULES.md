# 🔒 Правила безопасности репозитория

**Дата создания:** 2025-01-06  
**Критически важно для предотвращения утечек токенов и секретов!**

---

## 🚨 КРИТИЧЕСКИЕ ОШИБКИ (НЕ ДОПУСКАТЬ!)

### ❌ ЗАПРЕЩЕНО:

1. **Токены в git config (URL remote)**
   ```bash
   # НЕПРАВИЛЬНО:
   git remote set-url github https://ghp_TOKEN@github.com/user/repo.git
   
   # ПРАВИЛЬНО:
   git remote set-url github https://github.com/user/repo.git
   # Токен сохранится через Credential Manager при первом push
   ```

2. **Токены в файлах проекта**
   - Не коммитить `.env` файлы с токенами
   - Не хранить токены в `.md` файлах (кроме примеров с `ghp_...` → заменить на `ghp_EXAMPLE`)
   - Не оставлять токены в истории коммитов

3. **Публичный репозиторий с секретами**
   - Если репозиторий публичный → проверять ВСЕ файлы на наличие токенов
   - Использовать `git-secrets` или `git-hound` для сканирования
   - Перед каждым коммитом проверять: `grep -r "ghp_\|ghs_\|gho_" .`

4. **Токены в логах/консоли**
   - Не выводить токены в `console.log()`
   - Не логировать токены в файлы
   - Не показывать токены в терминале (использовать `git credential-cache`)

---

## ✅ ПРАВИЛЬНАЯ РАБОТА С ТОКЕНАМИ:

### 1. **GitHub Personal Access Token**

**Создание токена:**
- Минимальные права: `public_repo` + `workflow` (если используются Actions)
- НЕ использовать `repo` (полный доступ) для публичных репозиториев

**Сохранение токена:**
```bash
# Вариант 1: Windows Credential Manager (рекомендуется)
git config --global credential.helper manager-core
# При первом push GitHub попросит ввести токен, Windows сохранит его безопасно

# Вариант 2: SSH ключи (еще лучше!)
# Настроить SSH ключ и использовать:
git remote set-url github git@github.com:user/repo.git
```

### 2. **GitHub Actions Secrets**

Для CI/CD использовать GitHub Secrets:
- Settings → Secrets and variables → Actions → New repository secret
- НЕ хранить токены в `.github/workflows/*.yml` напрямую!
- Использовать: `${{ secrets.TOKEN_NAME }}`

### 3. **Environment Variables**

Для локальной разработки:
- Использовать `.env` файл (в `.gitignore`)
- Использовать `.env.example` для документации (БЕЗ реальных токенов)

---

## 🔍 ПРОВЕРКИ ПЕРЕД КОММИТОМ:

### Автоматическая проверка токенов:
```bash
# Проверить все файлы на наличие токенов GitHub
grep -r "ghp_[A-Za-z0-9]\{36,\}" . --exclude-dir=.git --exclude-dir=node_modules

# Проверить git config
git config --get-regexp remote.*url | grep -E "ghp_|ghs_|gho_"

# Проверить историю коммитов (если что-то попало)
git log --all --source --full-history -p | grep -E "ghp_|ghs_|gho_"
```

**✅ Цель: 0 совпадений!**

---

## 🛡️ РЕКОМЕНДАЦИИ:

### 1. **Репозиторий**

**Для личного блога:**
- ✅ Лучше использовать **приватный репозиторий**
- Если публичный → обязательно проверять все коммиты на токены

**Как сделать приватным:**
1. GitHub → Settings → Scroll down → Danger Zone
2. Change visibility → Make private

### 2. **Аутентификация**

**Приоритет методов (от лучшего к худшему):**
1. ✅ SSH ключи (рекомендуется)
2. ✅ GitHub Credential Manager (Windows/Git Credential Manager)
3. ⚠️ Personal Access Token (только если нет SSH)
4. ❌ Токен в URL (НИКОГДА!)

### 3. **Мониторинг**

**Регулярно проверять:**
- GitHub Security tab → Security advisories
- GitHub Settings → Security → Active sessions (незнакомые устройства)
- GitHub Settings → Developer settings → Personal access tokens (неактивные токены)

---

## 📋 ЧЕКЛИСТ ПЕРЕД ПУШЕМ В ПУБЛИЧНЫЙ РЕПО:

- [ ] Проверил `git config` на токены в URL: `git remote -v`
- [ ] Проверил файлы на токены: `grep -r "ghp_" .`
- [ ] Проверил `.env` в `.gitignore`
- [ ] Проверил историю коммитов (если подозрения)
- [ ] Убедился что репозиторий приватный ИЛИ все токены удалены

---

## 🚨 ЕСЛИ ТОКЕН ПОПАЛ В ПУБЛИЧНЫЙ РЕПО:

1. **НЕМЕДЛЕННО:**
   - Отозвать токен на GitHub: https://github.com/settings/tokens
   - Удалить токен из git config: `git remote set-url github https://github.com/user/repo.git`
   - Удалить из истории (если попал): `git filter-branch` или `git-filter-repo`

2. **ПРОВЕРИТЬ:**
   - Активность токена (GitHub Audit Log)
   - Не создавались ли новые репозитории
   - Не было ли несанкционированных push'ей

3. **СОЗДАТЬ НОВЫЙ ТОКЕН:**
   - С минимальными правами
   - Сохранить безопасно (Credential Manager)

---

## 📝 ПРИМЕРЫ ОШИБОК:

### ❌ Пример 1: Токен в git config
```bash
# БЫЛО (неправильно):
[remote "github"]
    url = https://ghp_TOKEN123@github.com/user/repo.git

# СТАЛО (правильно):
[remote "github"]
    url = https://github.com/user/repo.git
```

### ❌ Пример 2: Токен в файле документации
```markdown
# БЫЛО (неправильно):
Токен: ghp_EXAMPLE_TOKEN_REPLACE_WITH_YOUR_OWN

# СТАЛО (правильно):
Токен: ghp_EXAMPLE_TOKEN_REPLACE_WITH_YOUR_OWN
```

### ❌ Пример 3: Токен в GitHub Actions
```yaml
# БЫЛО (неправильно):
env:
  TOKEN: ghp_abc123...

# СТАЛО (правильно):
env:
  TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ:

- GitHub Personal Access Tokens: https://github.com/settings/tokens
- GitHub Security Best Practices: https://docs.github.com/en/code-security
- Git Credential Manager: https://github.com/GitCredentialManager/git-credential-manager
- SSH Keys Setup: https://docs.github.com/en/authentication/connecting-to-github-with-ssh

---

**ПОМНИ: Одна утечка токена = полный доступ к твоему GitHub аккаунту!**

**Обновлено:** 2025-01-06 после обнаружения токена в публичном репозитории
