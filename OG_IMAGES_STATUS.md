# 📊 СТАТУС OG ИЗОБРАЖЕНИЙ

**Дата:** 2026-01-20

---

## ✅ ЧТО СДЕЛАНО

1. **Создан скрипт генерации** (`scripts/generate-og-images.mjs`)
   - Генерирует OG изображения из HTML/CSS через `satori` + `@resvg/resvg-js`
   - Автоматически запускается после `npm run build`
   - Читает статьи из `src/content/` и создаёт изображения в `dist/og/`

2. **Обновлён `BaseHead.astro`**
   - Автоматически определяет путь к OG изображению
   - Формат: `/marketlab-academy/og/{lang}-{slug}.png`

3. **Добавлено логирование**
   - Скрипт выводит информацию о прогрессе
   - Показывает количество найденных статей и сгенерированных изображений

---

## 🔍 КАК ПРОВЕРИТЬ ПОСЛЕ ДЕПЛОЯ

### 1. Проверить GitHub Actions

**После пуша:**
- GitHub Actions → Actions → Последний workflow run
- Проверить логи сборки на наличие:
  - `[OG Images] Starting OG image generation...`
  - `[OG Images] Found X posts`
  - `[OG Images] Generated X images successfully`

### 2. Проверить наличие изображений

**После деплоя:**
- Открыть: `https://hampfree-hub.github.io/marketlab-academy/og/homepage.png`
- Должно открыться изображение 1200x630px

**Или проверить любую статью:**
- `https://hampfree-hub.github.io/marketlab-academy/og/ru-money-flow-index.png`

### 3. Проверить в Telegram

**После деплоя:**
1. Отправить себе ссылку на статью в Telegram
2. Должно показаться кастомное превью (не Astro placeholder)

**Важно:** Telegram кэширует превью. Если показывает старое:
- Использовать: https://developers.facebook.com/tools/debug/
- Вставить URL → "Scrape Again"

---

## ⚠️ ЕСЛИ НЕ РАБОТАЕТ

### Проблема: Изображения не генерируются

**Проверить:**
1. Логи GitHub Actions — есть ли ошибки?
2. Запустить локально: `npm run build` → проверить `dist/og/`
3. Проверить, что все зависимости установлены: `npm install`

### Проблема: Telegram показывает старое превью

**Решение:**
- Кэш Telegram обновляется через несколько часов
- Или использовать Facebook Debug Tool для принудительного обновления

---

## 📋 СЛЕДУЮЩИЕ ШАГИ

1. **Дождаться деплоя** (2-5 минут после push)
2. **Проверить логи** GitHub Actions
3. **Проверить изображения** по прямым ссылкам
4. **Проверить в Telegram** (после очистки кэша, если нужно)

---

**После деплоя проверим результат!** ✅
